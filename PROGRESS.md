# 한끗루틴 진행 상황

> 새 대화(세션)를 시작할 때 이 파일을 먼저 읽으면 지금까지 뭘 했고 뭐가 남았는지 바로 파악할 수 있어요.
> 실제 코드/DB 상태는 git 커밋과 Supabase가 정답입니다 — 이 문서는 "요약"일 뿐이니, 내용이 코드와 다르면 코드를 신뢰하세요.

## 프로젝트 구조

- `index.html` — 메인 앱 (청소년/끗짱 공용 SPA, GitHub Pages 배포)
- `admin.html` — 관리자 전용 웹페이지 (참가자 관리, 루틴 관리). 로그인은 앱과 동일한 Supabase Auth 사용, `ADMINS` 배열로 접근 제한
- `landing.html` — 마케팅 랜딩페이지
- `supabase-schema.sql` — DB 스키마 + **모든 마이그레이션 기록** (파일 맨 아래로 갈수록 최신). 새 변경사항은 항상 이 파일 끝에 `[마이그레이션 YYYY-MM-DD]` 블록으로 추가하고, Supabase SQL Editor에서 그 블록만 실행하는 방식으로 운영 중
- 배포: GitHub Pages, repo `youthitdev/youthit-routine`, 브랜치 `main`
- DB: Supabase (`ynqvhsffoesjzefitafv` 프로젝트)

## 역할 & 관리자

- 역할: `youth`(청소년), `kkutjjang`(끗짱)
- **관리자 이메일 (dev@youthvoice.or.kr, yv@youthvoice.or.kr)** — `index.html`의 `ADMINS` 배열, `admin.html`의 `ADMINS` 배열, DB `is_admin()` 함수 **3곳 모두 동일하게 유지해야 함**. 하나라도 다르면 "화면엔 관리자로 보이는데 실제 DB 쓰기는 실패"하는 버그가 생김 (실제로 한 번 발생했었음)
  - `ai@youthvoice.or.kr`은 2026-07-29 지원사업 심사위원용으로 잠깐 관리자로 추가했다가, 심사위원에게는 일반 끗짱 계정을 주기로 해서 다시 제외함
  - `youthpd@youthvoice.or.kr`은 심사위원용 청소년 계정 — 실제 데모에는 대표님이 직접 신청한 기존 루틴("오늘 노래 추천 루틴")을 쓰기로 해서, 처음 만들어뒀던 "☀️ 심사용 데모 루틴"(ai@ 담당)은 2026-07-30에 삭제함(참여자·인증 기록도 CASCADE로 함께 삭제됨)
- 관리자는 `state.loginType='both'`로 청소년/끗짱 화면을 전환하며 이용 가능

## 완료된 주요 기능

### 가입 · 인증(계정) 체계
- 가입 시 기본정보(닉네임/실명/생년월일/휴대폰/지역 시도·시군구/역할) 수집, 청소년은 재학상태(학교밖/재학/대안학교)까지
- **끗짱 가입 승인제** — 가입 즉시 이용 불가, 관리자 승인(`kkutjjang_status`) 후 이용 가능. 관리자 이메일은 자동 승인
- 학교밖청소년 서류 제출 → 관리자 승인 플로우 (Storage 비공개 버킷)
- 재학상태별로 요구 서류 안내 차등화 (학교밖: 정원외관리증명서 등 5종 / 재학: 청소년증·학생증)
- 승인되면 이름 옆 "✅ 학교밖청소년 인증" 뱃지 노출
- **비밀번호 찾기/재설정** 플로우 (Supabase resetPasswordForEmail + 재설정 화면)
- 프로필 편집(닉네임/실명/생년월일/휴대폰/지역, 청소년은 재학상태도) — Supabase에 실제 반영

### 루틴
- 루틴 생성/수정 (끗짱), 참여자격 분리("청소년 누구나" / "학교밖청소년 전용")
- **루틴 개설은 관리자만** 가능(당분간), 관리자가 담당 끗짱(`led_by`)을 지정
- 담당 끗짱은 자기 루틴만 보고 관리(홈/루틴탭/인증탭/통계 전부 스코핑됨), 관리자는 전체 조회
- 참여 신청 시 각오/소감 입력, 끗짱이 승인/거절
- **참여 승인 안 된 사람은 인증 불가** (클라이언트+RLS 이중 방어 — 한 번 뚫려있던 취약점 수정함)
- OT(첫날)/클로징(마지막날) 온라인 모임 일정 + 줌 링크, 구글캘린더 저장 연동
- 루틴 상태(`recruit→active→done`) 중 `active→done`은 **종료일 기준 자동 전환** (클라이언트 계산 + 권한 있는 사용자가 조회 시 DB에도 반영)
- 파트너 기관명, 루틴 대표 이미지, 카드 UI(이미지 중심 디자인)
- **루틴 보관/삭제** — admin.html에서 보관(숨김) 또는 완전 삭제 가능

### 인증(루틴 인증) · 커뮤니티
- 인증 사진(최대 5장, Storage 업로드)·소감 실제 저장 (예전엔 저장 안 되던 버그 있었음)
- 인증 피드가 **실데이터 연결됨** (예전엔 `CERT_FEED`라는 하드코딩 목업이었음) — 좋아요/댓글 실시간 반영
- 끗짱도 자기 루틴을 청소년처럼 직접 인증 가능
- 게시글/댓글 삭제 — 본인 또는 관리자만 (RLS + UI)

### 관리자 페이지 (admin.html)
- 청소년/끗짱 목록 검색·필터(지역/재학상태/서류상태/참여루틴)
- 일괄 승인/반려 (청소년 서류, 끗짱 가입)
- CSV 내보내기
- 루틴 탭: 상태 필터, 보관/삭제
- **통계 대시보드** (`📊 통계` 탭, Chart.js 사용) — KPI(청소년/끗짱 수, 진행중 루틴, 전체 완주율), 지역별 청소년 분포/재학상태 비율/루틴 상태 분포 차트, 루틴별 완주율 테이블. 완주율은 각 루틴의 실제 기간(`end_date - start_date`) 기준 2/3 이상 인증 여부로 계산 (기존 index.html의 21일 하드코딩 로직을 일반화함)

### 기타
- 모바일 하단 탭이 안 고정되던 버그 수정 (`.app`의 `transform: translateZ(0)`가 원인)
- 랜딩페이지 브랜딩(YouthVoice 표기, 로고, 파비콘)
- 홈 화면 "이번 주 한끗" 전역 섹션 제거 → "참여 중인 루틴" 각 카드에 루틴별 주간 인증 그리드로 통합 (2026-07-10, 스트릭 배지·진행률과 중복돼 보여서 정리, 루틴이 보통 1~2개뿐이라 루틴별로 나누는 게 더 명확하다는 논의 끝에 결정)

## 남은 작업 (우선순위순, 2026-07-11 재정리)

1. ~~알림 켜기 유도 + iOS 홈 화면 추가 안내~~ — ✅ 완료 (2026-07-11). ①홈 상단에 알림 미설정자용 유도 배너(켜기 버튼, ✕ 누르면 7일 숨김, 권한 차단자에겐 노출 안 함) ②참여 신청 완료 직후 알림 설정 시트 자동 오픈 ③iOS 미설치자에겐 "홈 화면에 추가" 방법 안내 배너. 구독 로직 `_subscribePush()` 공용화, `_getPushSubscriptionSafe()`(1.5초 타임아웃)로 SW 미동작 환경에서도 안전
2. ~~회원 탈퇴 기능 + 개인정보 동의 절차~~ — ✅ 완료 (2026-07-14). 탈퇴 기능 배포·실기기 테스트 완료([[11]] 참고). 개인정보 수집·이용 동의 문구 확정(제미나이 초안을 실제 수집 항목 기준으로 검증·수정 — 실명/생년월일/휴대폰/지역/재학상태/학교밖청소년 서류/인증사진·소감/참여신청 각오·소감 반영, "기기 식별자(FCM)"를 실제 Web Push 구독 정보로 정정, 미구현 소셜 로그인 문구 제거, "만 14세 이상" 조항 추가) → 가입 화면에 필수 체크박스 2개(만 14세 이상/개인정보 동의, "자세히" 누르면 방침 전문 시트) + 선택 체크박스 1개(마케팅 동의) 실제 반영, 브라우저에서 검증 완료(index.html:342-353 UI, index.html:1315-1345 doSignup 검증 로직). `doSignup()`에 생년월일 기준 만 14세 미만 가입 차단 로직도 추가(index.html:1327-1329). DB에 `profiles.privacy_agreed_at`(가입=동의 시점 자동 기록)·`profiles.marketing_agreed` 컬럼 추가 마이그레이션(`supabase-schema.sql` `[마이그레이션 2026-07-14]`) 실행 완료
3. ~~완주 리포트 이미지 생성 → SNS 공유~~ — ✅ 완료 (2026-07-11). MY 탭 "완주한 루틴" 항목마다 "📤 공유" 버튼 → 브랜드 그라데이션 카드(로고+루틴 이모지+총 일수/인증 횟수/완주율) 미리보기 시트 → html2canvas로 PNG 이미지화(scale 3배) → 모바일은 Web Share API로 바로 공유, 미지원 환경은 다운로드 폴백. CDN 의존성 추가: html2canvas. 부수 수정: "완주한 루틴" 목록이 참여 여부 무관하게 상태만 보고 전부 표시하던 버그 수정(본인 참여 루틴만 표시)
4. ~~리워드 시스템 (나다움 포인트 + 상점)~~ — ✅ 1차 구현 완료 (2026-07-14, DB 마이그레이션 실행 완료). 설계 확정 사항: 루틴별 "완주 리워드" 개념은 없음(끗짱 요청으로 폐기) — 대신 활동별 고정 포인트(인증 10/댓글 1/후기 30, 기존 그대로)를 상점 포인트로 그대로 사용. 리워드 교환은 상품권 지급 전제라 배송 주소는 안 받고 기존 `profiles.real_name/phone`만 사용. 상시 루틴(기한 없는 자유참여)은 이번 범위에서 제외.
    - DB: `rewards`(title/description/cost/stock/image_url/active), `reward_redemptions`(user_id/reward_id/status/requested_at/fulfilled_at) 신규 테이블. `routine_participants.nadaeum_paid` 컬럼 추가(완주 지급 중복 방지)
    - **나다움 인증 지급 시점 변경**: 댓글(+1)/후기(+30)는 기존처럼 즉시 지급 유지. **인증(+10)만** 화면 토스트는 즉시 보여주되 실제 DB 반영은 서버 트리거(`on_cert_nadaeum_payout`, AFTER INSERT ON certifications)가 그 루틴 완주 기준(2/3) 최초 달성 시 누적분을 한 번에 지급, 이후 추가 인증은 즉시 지급. `save()`가 매번 `nadaeum`을 덮어쓰던 걸 제거해서 트리거 지급분이 클라이언트에 씹히지 않게 함(index.html:1410-1420, 2446-2469, `submitCert()`)
    - **리워드 교환 서버 가드**: `guard_redemption` 트리거(BEFORE INSERT ON reward_redemptions)가 잔액·재고·활성여부 확인 후 원자적으로 차감(`guard_verify_status` 패턴 재사용), 조건 위반 시 신청 자체를 막음
    - admin.html "🎁 리워드" 탭: 리워드 등록/활성화토글/삭제, 교환 신청 목록(신청자 이름·연락처·상태, 발송완료 처리), 참가자 수동 포인트 지급(사유 입력, `nadaeum_log`에 기록)
    - index.html MY탭 나다움 카드 아래 "🎁 상점" 진입 → 리워드 목록(잔액·재고 부족 시 버튼 비활성화)/교환하기/내 교환 내역
    - 브라우저 콘솔로 상점·관리자 화면 렌더링 전부 확인(잔액부족/품절/활성 여부 버튼 상태 포함). DB 마이그레이션(`[마이그레이션 2026-07-14c]`) 실행 완료
    - **포인트 소멸기간 3개월** (2026-07-14 추가 확정) — `nadaeum_log`를 장부 삼아 선입선출 계산: 매일 00:10 pg_cron 잡(`hankkut-nadaeum-expiry`)이 "3개월 지난 적립 총액 − 쓴 총액" 초과분을 소멸 처리(`expire_nadaeum()`, 소멸도 음수 로그로 남아 멱등). 댓글/후기 적립이 로그를 안 남기던 것도 `earnNadaeum()`에서 기록하도록 수정. 도입 전 기존 잔액은 마이그레이션 시점 적립분으로 백필(오늘부터 3개월 뒤 소멸 대상). 상점 화면에 "적립 후 3개월이 지난 포인트는 소멸돼요" 안내 추가. 마이그레이션 `[2026-07-14d]`
    - **리워드 마무리 4종** (2026-07-14 저녁, 마이그레이션 14e 실행 완료) — ①상점에 "포인트 내역" 섹션(적립 파랑/교환 빨강/소멸 회색, 최근 50건) ②소멸 임박 인앱 배너: 30일 내 소멸 예정 포인트를 클라이언트에서 선입선출 근사 계산(`_calcExpiring30d()`), 상점+MY탭 양쪽 노출, 소멸분 없으면 숨김 (푸시 알림은 피로도 우려로 안 하기로 결정, 발송완료 푸시·재고 임박 표시도 제외 확정) ③완주 지급 순간 푸시: 트리거에 `notify_push()` 추가 — 루틴명+지급 총액 알림, 완주 후 추가 인증(+10)은 알림 없음 (마이그레이션 `[2026-07-14e]`) ④admin 리워드 탭 상단 KPI: 교환 건수(대기 N건)+유통 포인트(청소년 잔액 합계). 브라우저 검증 완료(소멸 계산·배너 숨김 조건·KPI 렌더링)
    - **남은 것**: 실기기 종단 테스트 — ①인증 쌓아서 완주 기준 넘겼을 때 나다움 일괄 지급되는지 ②상점에서 교환→잔액·재고 차감→admin 교환신청 목록 표시→발송완료 처리 ③관리자 수동 지급. 스태프 테스트 때 함께 확인 권장
5. ~~OT 때 "함께 인증" 실습 모드~~ — ❌ 안 하기로 결정 (2026-07-14)
6. ~~인증 사진 타임스탬프 촬영 (선택 기능)~~ — ✅ 완료 (2026-07-14 저녁). 인증 폼 사진 그리드에 "📸 지금 찍기" 타일 추가(초록 점선, 갤러리 + 타일 옆) — `capture="environment"` input으로 모바일에서 카메라 바로 열림(데스크톱은 파일 선택으로 폴백). 촬영한 사진에 canvas로 "📸 YYYY.MM.DD HH:MM 실시간 인증" 워터마크를 우하단에 새겨서 저장(`handleCertCameraPhoto()`, jpeg 0.85). 갤러리 업로드는 그대로 유지, 강제 아님. 브라우저에서 합성 이미지로 워터마크 렌더링 확인 완료
7. ~~정리거리~~ — ✅ 완료. approval 프로젝트 `push_subscriptions`는 확인 결과 존재하지 않음(이미 정리됐거나 애초에 미생성, 2026-07-15 확인). git 커밋 작성자 설정도 완료 (2026-07-14, `김재순 <soon@youthvoice.or.kr>`)
8. ~~끗짱용 완주 리포트 공유~~ — ✅ 완료 (2026-07-14 저녁). 끗짱 루틴 상세(완료 루틴)에 "📤 완주 리포트" 버튼 → 핑크 그라데이션 카드(끗짱 브랜드 컬러, 청소년용 파란 카드와 구분): "청소년 N명과 함께 완주했어요" + 함께한 청소년 수/모두의 인증 수/완주 달성률(완주 기준 2/3 넘긴 인원 비율). `_kReportCardHtml()`/`openKReportCard()`, 기존 시트·html2canvas 캡처·공유 함수 그대로 재사용. 브라우저 검증 완료(15일 루틴 4명 중 3명 달성 = 75% 계산 확인)
9. ~~완주 축하 화면(confetti) 미연결~~ — ✅ 완료 (2026-07-14). 로그인 시(`loadProfileAndEnter`)와 백그라운드 복귀 재조회 시(`refetchOnResume`) 양쪽에서 `_maybeCelebrateCompletion()` 호출 → 현재 유저가 참여자인 `done` 루틴 중 아직 안 축하한 걸 찾아 `openCompleteSuccess()` 자동 오픈(index.html:3821-3829). "이미 축하했는지"는 로컬스토리지 `hankkut_celebrated_{userId}_{routineId}` 플래그로 추적, 한 번에 하나만 노출. 부수 수정: `openCompleteSuccess()`가 원래 루틴 전체 인증수(`r.certCount`)를 써서 개인 통계가 아니었던 버그를 `openReportCard()`와 동일하게 참여자 본인 인증수 기준으로 수정. 브라우저 콘솔로 상태 주입해 동작·중복 방지 모두 확인함. 축하 화면에 "📤 완주 리포트 공유" 버튼도 추가해 축하→공유 흐름 연결 (2026-07-17)
10. ~~회원 탈퇴 시 운영진 알림 없음~~ + 11. ~~탈퇴 통계용 익명 로그~~ — ✅ 완료 (2026-07-14 저녁, 마이그레이션 14f 실행·Edge Function 재배포 완료). ①`withdrawal_log` 테이블(역할+탈퇴시각만, 이름/연락처 없음, RLS는 관리자 SELECT만·INSERT는 service role 전용) ②`notify_admins(title, body)` DB 함수 — 관리자 이메일 전원에게 `notify_push()` ③`delete-account` Edge Function 수정: 삭제 전에 역할·닉네임 읽어두고, **삭제 성공 후에만** 익명 로그 1행 insert + 관리자 푸시("청소년 OO님이 탈퇴했어요" — 닉네임은 푸시에만 쓰고 어디에도 저장 안 함) ④admin 통계 탭에 "탈퇴" KPI 카드(누적/최근 30일). 배포: 사용자가 직접 `supabase functions deploy delete-account` 실행 필요
12. ~~최소 인원 미달 폐강 (수동)~~ — ✅ 완료 (2026-07-14, DB 마이그레이션 실행 완료). 구현 내용:
    - 루틴 만들기/수정 폼에 "최소 참여 인원"(선택) 입력 추가(index.html:858-859), `min_people` 없으면 폐강 기능 자체를 안 씀
    - DB: `routines.min_people` 컬럼 + `status` CHECK에 `'cancelled'` 추가, `routines` UPDATE 트리거로 폐강 시 신청자(대기+승인) 전원에게 `notify_push()` 자동 발송 (`supabase-schema.sql` `[마이그레이션 2026-07-14b]`)
    - 배지: 청소년 카드/상세, 끗짱 카드/상세, admin.html 테이블 전부에 회색 "폐강" 표시 + 상세화면 "최소 인원(N명) 미달로 폐강됐어요" 문구
    - "폐강 처리" 버튼: index.html 끗짱 루틴 상세(`cancelRoutine()`, min_people 설정된 recruit/active 루틴에만 노출) + admin.html 루틴 탭(`cancelRoutineAdmin()`, 기존 보관/삭제 버튼과 같은 자리)
    - 브라우저 콘솔로 상태 주입해 3개 화면(청소년/끗짱/관리자) 배지·버튼 노출조건 전부 확인
13. ~~가입 시 카카오 로그인 추가~~ — ✅ 완료 (2026-07-15, 실제 로그인 테스트 성공). 구현: ①로그인/가입 탭에 "💬 카카오로 시작하기" 버튼(`signInWithKakao()`, `signInWithOAuth({provider:'kakao'})`) ②최초 소셜 로그인 감지: `loadProfileAndEnter()`에서 기본정보(실명/생년월일/휴대폰/지역) 비면 추가 정보 입력 화면(`completeProfileScreen`)으로 — 역할 선택+기본정보+동의 체크박스 3종+14세 검증, 카카오 닉네임 프리필, 완료 시 `privacy_agreed_at` 기록 후 정상 진입(끗짱 선택 시 `kkutjjang_status='pending'` — guard 트리거가 pending 설정은 허용함) ③`handle_new_user` 트리거: 소셜 가입은 동의 전이므로 `privacy_agreed_at` NULL로 생성(birth_date 메타 유무로 이메일/소셜 구분), 카카오 `full_name` 메타도 닉네임 폴백 (마이그레이션 `[2026-07-15]` 실행 완료). 브라우저 검증 완료(화면 전환·프리필·검증 3종). 외부 설정도 완료 (2026-07-15): ⓐKakao Developers 앱 등록, 카카오 로그인 ON, REST API 키 상세에서 리다이렉트 URI 등록(새 콘솔은 "앱 설정→플랫폼 키→REST API 키" 안에 있음, 클라이언트 시크릿도 같은 페이지 하단에 기본 활성화돼 있음) ⓑSupabase Kakao provider에 Client ID/Secret 입력 ⓒRedirect URLs에 배포 주소 등록 ⓓ동의항목: 닉네임 필수·프로필사진 선택. **KOE205 트러블슈팅 기록**: Supabase 서버가 account_email scope를 항상 요청하는데 일반 앱은 이메일 동의항목 권한이 없어 실패 → **"개인 개발자 비즈 앱 전환"**(사업자등록 불필요, 앱 설정→비즈니스) 후 이메일 동의항목을 "선택 동의"로 설정해 해결(카카오계정 정보 수집 후 제공 체크는 안 함). 로그인→추가정보 화면→진입 실제 테스트 성공, 동의 체크박스 간격도 조정. 노출됐던 시크릿 코드는 재발급 + Supabase 갱신 완료

### 최근 완료 (2026-07-09~11 스프린트)

- 관리자 통계 대시보드(Chart.js) + 루틴별 CSV 출석부(체크박스 다중 선택, 기간 밖 인증 포함 버그 수정)
- **Web Push 알림 인프라 4단계 전체** — 구독/발송 파이프라인, 자동 트리거(승인·거절·좋아요·댓글), 관리자 알림 보내기 탭(전체/루틴별+메시지 템플릿 저장), 마일스톤 축하(5/10/15/20회)·리마인드 시간 설정(pg_cron). 모두 실기기 검증
- 거절 사유 안내(서류/끗짱 가입/루틴 참여 3종, 마이그레이션 2026-07-11b 실행 완료) — 루틴 거절이 신청자에게 안 보이던 버그도 함께 수정("거절됨"+사유+"다시 신청하기")
- 아이폰 safe-area 잘림 수정, 숨김 화면 데스크톱 노출 버그 수정, 백그라운드 복귀 시 자동 재조회, 참가자 승인 취소, OT/클로징 배너 당일만 노출, 홈 "이번 주 한끗"→루틴 카드별 주간 그리드 통합, 카테고리 기능 삭제, 루틴 탭 "참여 중" 기준 홈과 통일, 주간 그리드·셀렉트 디자인 개선

## 참고: 알림 인프라 운영 정보

- 구축된 것: `push_subscriptions` 테이블+RLS, `send-push` Edge Function(관리자 JWT 또는 service 키만 발송, 만료 구독 자동 정리, `user_id`/`user_ids`/`target:'all'`/`target:'routine'+routine_id` 지원, CORS 헤더 포함), VAPID 시크릿, 마이페이지 "알림 설정" 토글+리마인드 시간(`profiles.reminder_hour`), service-worker.js push/notificationclick 핸들러(캐시 v4), DB 트리거(승인/거절·좋아요·댓글·마일스톤), pg_cron 잡 `hankkut-cert-reminder`(매시 정각, `send_cert_reminders()`)
- 발송: `POST {SB_URL}/functions/v1/send-push` + `Authorization: Bearer <service키 또는 관리자 JWT>` + `{"user_id"|"user_ids"|"target":"all"|{"target":"routine","routine_id":N}, "title", "body", "url"}`. DB에서 직접: `SELECT notify_push(user_id, title, body);`
- 디버깅: 발송 로그 `SELECT * FROM net._http_response ORDER BY id DESC LIMIT 5;`. 브라우저에서 함수 호출 시 CORS 헤더 필수
- **"알림 설정은 켜져 있는데 실제 푸시가 안 온다"는 문의 시 1차 해결책** (2026-07-22 실기기로 확인) — 인앱 알림 내역함(`notifications` 테이블)에는 기록되는데 폰에 배너/알림센터로는 안 오는 경우, iOS PWA의 구독 정보가 깨져있을 가능성이 큼. **홈 화면 아이콘을 삭제하고 다시 추가(재설치)** 하면 새 구독이 맺어지면서 해결됨. 앱 안에서 "알림 끄기→켜기"만으로는 해결 안 될 수 있음(겉보기엔 켜진 것처럼 보여도 구독이 안 살아남). 스태프 테스트나 실사용자 문의에서 우선 안내할 것
- 로컬 도구: Deno(`~/.deno/bin`)·Supabase CLI(`~/.local/bin`, login+link 완료) — PATH 미등록, 쓸 때 `export PATH="$HOME/.deno/bin:$HOME/.local/bin:$PATH"`. VAPID 키 원본 `~/.hankkut-vapid-keys`(**비밀키 절대 저장소에 넣지 말 것**)
- 주의: SQL Editor 실행 시 **프로젝트가 ynqvhsffoesjzefitafv인지 확인**(approval 프로젝트에 실행한 사고 있었음). iOS는 "홈 화면에 추가"해야 푸시 동작(16.4+), 집중 모드 중엔 알림 센터에만 쌓임. 새 테이블 후 API 캐시 갱신에 `NOTIFY pgrst, 'reload schema';` 필요할 수 있음

## 아이디어 백로그 (논의만 됨)

- **파트너 리스트 / 청소년 갈 만한 공간(지도 등)** — 하단 탭이 이미 5개로 꽉 차서 새 탭 추가는 지양. 파트너 리스트는 커뮤니티 탭 안 섹션으로, 지도는 홈 카드로 시작해 반응 보고 탭 승격 검토 (2026-07-10 논의)
- 파트너 기관별 성과 모아보기

## 최근 완료 (2026-07-22, AI 루틴 리포트 실제로 작동하게 고침)

- **발견**: MY탭 "AI 루틴 리포트" 기능이 코드는 있었지만 실제로는 작동 안 했음 — `openReport()`가 `https://api.anthropic.com/v1/messages`를 **브라우저에서 직접** 호출하는데 API 키가 어디에도 없었음(설령 넣어도 클라이언트에 노출되는 구조라 그대로 쓰면 안 됨)
- **수정**: `send-push`/`delete-account`와 동일한 패턴으로 `ai-report` Edge Function 신설 — 여기서만 `ANTHROPIC_API_KEY`(Supabase Secrets)를 보관하고 Anthropic을 호출, 클라이언트는 본인 JWT로 이 함수만 호출(`supabase/functions/ai-report/index.ts`). 모델은 `claude-sonnet-5`로 지정
- **게이팅 추가** (2026-07-22 요청) — 데이터가 없으면 분석이 의미가 없어서, **루틴 3개 참여 또는 인증 30회 중 하나를 채워야** 리포트를 받을 수 있게 함(`_aiReportProgress()`). 미달성 시 MY탭 카드에 "루틴 참여 N개 또는 인증 N회를 더 채우면..." 안내+버튼 잠금 표시, 그래도 눌러서 시트를 열면 같은 안내가 나옴(막다른 길이 아니라 동기부여로 보이게)
- **완주 축하 화면에 진입 링크 추가** — 완주 직후가 "내 기록을 돌아보고 싶은" 동기가 가장 높은 순간이라 판단, 기존 3버튼(편지/리포트공유/나중에) 사이에 "📊 내 루틴 AI 리포트도 보러가기" 텍스트 링크 추가
- 브라우저에서 미달성 안내문구·잠금 시트·조건 충족 후 Edge Function 호출(URL·인증헤더·프롬프트·응답 파싱)·완주화면 링크까지 전부 확인. **배포 필요**(사용자가 직접): `supabase functions deploy ai-report` + `supabase secrets set ANTHROPIC_API_KEY=<발급받은 키>` — console.anthropic.com에서 별도 API 키 발급 필요(끗짱/관리자 개인 계정과는 별개의 유료 API 키)

## 최근 완료 (2026-07-22, QA 이슈트래커 2차 대응)

이슈트래커 6건 중 5건 처리(1건은 사용자 확인 대기):
1. **참여중 루틴 인증글 클릭 시 인증 버튼 없음** — `openRoutineFeed()`의 피드 그리드에서 인증 버튼(`certFloatingBtn`)을 보여주는 로직이, 홈/커뮤니티 등 다른 경로로 인증글 상세(`openCertFeedDetail`)에 바로 들어오면 적용이 안 됐던 문제. 해당 함수에도 동일한 노출조건(내 루틴이면서 오늘 미인증)을 추가하고, `routineDetailOverlay` 닫힐 때 버튼도 항상 숨기도록 정리(다른 화면에 잘못 남아있지 않게)
2. **인증 피드 빈 상태 문구 변경** — "아직 오�디 인증한 친구가 없어요" → "🐈 지나가던 고양이가 첫 인증글을 기다리는 중... 🐾"
3. **로그인된 상태에서 비밀번호 자동완성 팝업 뜨는 버그** — 로그인 화면(`.splash`)이 숨겨질 때 `opacity:0`만 적용돼서 비밀번호 입력창이 계속 DOM상 "보이는" 상태로 남아있던 게 원인으로 추정. 페이드 끝난 뒤 `visibility:hidden`도 적용되도록 수정(애니메이션은 그대로, 끝나고 나서 완전히 숨김) — 브라우저가 그 비밀번호 필드를 더 이상 채움 대상으로 안 보게 됨
4. **인증예시사진 사이즈를 루틴 화면과 통일** — 확인 필요, 하단 참고
5. **홈 화면 양쪽에 미세한 선 보임** — 데스크톱에서 폰 프레임처럼 보이려고 걸어둔 `max-width:390px`가, 실제 폰 화면(뷰포트 자체가 이미 ≤390px)에서는 뷰포트-앱 폭 사이 서브픽셀 오차로 미세한 선을 만들던 게 원인. `@media (max-width:390px)`에서 `.app`/`.nav`/`.overlay`/`.sheet`/`.success-scr`/`.complete-scr` 전부 `max-width:100%`로 풀어서 실제 폰에서는 그 계산 자체가 필요 없게 함
6. **커뮤니티 "+" 버튼이 하단 네비에 가려짐** — 하단 네비(`.nav`)가 `padding-bottom:env(safe-area-inset-bottom)`으로 노치 기기에서 더 커지는데, "+" 버튼은 고정된 `bottom:80px`라 노치 기기에서 네비(z-index:100)에 일부 가려졌던 것. 버튼 위치에도 동일하게 `+ env(safe-area-inset-bottom)` 더하고 z-index를 101로 올림(청소년/끗짱 커뮤니티 글쓰기 버튼 둘 다)

브라우저에서 1·2·3·5·6 전부 검증 완료. DB 변경 없음.

**4번은 보류** (2026-07-22) — "루틴 화면"이 상세 페이지(150px)인지 목록 카드(132px, 2단 그리드 구조상 비율이 다름)인지 확인 요청했으나, 둘 다 동시에 맞출 수 없는 구조라 "맞추지 말고 일단 해보자"로 결정 — 지금 상태(상세 페이지·배너와는 150px로 통일됨, 목록 카드만 구조상 다름) 그대로 유지, 추가 변경 없음.

## 최근 완료 (2026-07-22, 홈 화면 추가 정리)

- **헤더 "Youth-it" 삭제 + 정렬 정리** — 좌측 텍스트가 두 줄(Youth-it/한끗루틴)이라 우측 스트릭 배지·벨과 세로 중심이 안 맞았는데, "Youth-it" 줄을 없애고 "한끗루틴" 한 줄만 남겨서 자연스럽게 가운데 정렬됨
- **"오늘 한끗 완료" 배너 삭제** — 참여 중인 루틴을 그날 다 인증하면 뜨던 초록 축하 배너. 바로 아래 "참여 중인 루틴" 목록에 이미 "오늘 인증 ✓"로 표시되니 중복이라 판단, 완전히 제거(그 상태에선 이제 아무것도 안 뜸)
- **알림 미확인 점 색상 변경** — 빨강(#FF006C) 배경+파랑 테두리 → **노란색(#FFD600) 배경+노란색 테두리**로 변경
- **홈 인사말 박스 삭제 + 스트릭을 헤더로 이동** — "김유스님" 인사말 박스(파란 히어로 카드)를 완전히 삭제. 다만 그 안의 스트릭 배지("🔥 N일째")는 매일 앱을 열게 만드는 동기부여 요소라서 없애지 않고, 모든 탭에 공통으로 뜨는 상단 헤더(🔔 알림벨 옆)로 옮김 — `updateHeaderStreakBadge()`, 청소년일 때만+연속 인증 중일 때만 노출(0일째면 배지 자체를 숨김, 끗짱 화면에선 안 보임). 로그인 시·백그라운드 복귀 시·홈 렌더링 시 갱신. 브라우저에서 3일째/0일째/끗짱 케이스 전부 확인. 홈 화면은 이제 배너부터 바로 시작함

## 최근 완료 (2026-07-22, QA 이슈트래커 대응)

스태프 테스트 이슈트래커에서 올라온 4건 처리:
1. **서류 업로드 후 재로그인 시 버튼 재활성화** — 코드 리뷰로는 결함을 못 찾음(`renderVerifyCard`/`loadProfileAndEnter`의 `verify_status` 처리 로직은 정상). 대신 일반적으로 더 견고하게: MY탭 진입할 때마다 서버에서 `verify_status`를 다시 조회하는 `refreshVerifyStatus()` 추가(index.html, `switchTab`의 'my' 분기) — 로그인 이후 관리자가 승인/반려해도 재로그인 없이 최신 상태로 반영됨. 재현이 다시 되면 정확한 재현 절차(어느 계정, 언제 로그아웃했는지) 필요
2. **홈 알림벨이 "알림 내역" 대신 "알림 설정"만 보여줌** — 이 앱엔 원래 인앱 알림 목록(inbox) 자체가 없었음(Web Push만 OS 알림센터에 쌓이는 구조). `notifications` 테이블 신규 + `notify_push()` 함수 자체에서 그 테이블에 기록하도록 수정 → 기존 알림 트리거 10여 곳(승인/거절/좋아요/댓글/폐강/완주지급/콕찌르기/탈퇴 등) 전부 하나도 안 건드리고 자동으로 알림 내역에 쌓이게 됨. 홈 알림벨 클릭 시 `openNotifHistory()`로 최근 30건 목록 시트 노출(안 읽은 건 배경색 구분, 열면 자동 읽음처리), 안 읽은 알림 있으면 벨에 빨간 점 표시. 시트 안에 "⚙️ 알림 설정" 링크로 기존 설정 화면 계속 접근 가능. 마이그레이션 `[2026-07-22c]` 실행 완료
3. **끗짱이 참여 승인해도 청소년에게 알림이 안 옴** — `approveApplicant()`와 트리거(`on_participant_status_change`) 코드를 다시 읽어봐도 로직 결함을 못 찾음(pending→approved 전환 감지, `notify_push` 인자 모두 정상). 운영 DB에 트리거가 드리프트됐을 가능성에 대비해 동일 정의로 안전하게 재실행하는 걸 마이그레이션에 포함(`[2026-07-22c]`, 멱등). 그래도 안 오면 그 청소년 계정이 실제로 알림을 켰는지(마이페이지 알림 설정 ON, iOS는 홈 화면 추가 필수)부터 확인 권장 — 이제는 알림 내역함(위 2번)에도 안 쌓이는지 보면 "발송 자체가 안 됨" vs "발송은 됐는데 푸시만 안 옴"을 구분할 수 있음
4. **루틴 참여 신청 완료 팝업에 OT 안내 삭제** — 신청 직후 뜨는 확인창에서 "🚀 첫날 OT: ..." 문구 제거(아직 승인도 안 된 시점에 보여주는 게 부적절). "참여 신청이 완료됐어요! 끗짱의 승인 후 루틴에 참여할 수 있어요."만 남음. DB 변경 없음

브라우저에서 4건 모두 검증 완료(알림 내역함 목업 데이터 렌더링·읽음처리·안읽음점, 신청완료 알럿 문구 확인).

## 최근 완료 (2026-07-22, 이어서)

- **끗짱 "콕 찌르기" (미인증 청소년 응원)** — ✅ 완료. 끗짱 루틴 피드(`openKRoutineFeed`)의 미인증 참여자 타일에 "👋 콕" 버튼 추가 → 누르면 그 청소년에게 응원 푸시 발송("OO님이 '루틴명'에서 응원을 보냈어요. 오늘 한 끗 남겨볼까요?"). `pokes` 테이블에 `(routine_id, to_user, poke_date)` unique 제약을 걸어서 **하루 한 명당 한 번만** 가능하게 DB 레벨에서 방지(중복 시도하면 23505 에러를 클라이언트가 잡아서 "오늘 이미 콕 찔렀어요" 안내). INSERT 정책은 그 루틴의 담당 끗짱(`led_by`) 또는 관리자만 허용. 마이그레이션 `[2026-07-22b]` 실행 완료. 브라우저에서 버튼 노출조건·정상 발송·중복 차단 알림 전부 확인

## 최근 완료 (2026-07-17)

- **홈 화면 홍보 배너 + 히어로 카드 축소** — 관리자가 admin.html "📢 배너" 탭에서 등록(제목/내용/이미지URL/링크/노출순서, 활성·비활성 토글, 삭제). 루틴 홍보든 사용설명서든 외부 링크든 자유롭게. 홈 화면엔 active인 배너 중 `sort_order` 가장 작은 것 1개만 노출, 없으면 배너 영역 자체가 숨겨짐(기존 알림 유도 배너·OT 배너와 동일한 조건부 노출 패턴). `banners` 테이블 신규 (마이그레이션 `[2026-07-17]` 실행 완료)
  - 이 작업을 하면서 홈 히어로 카드도 함께 정리: 레벨 배지·레벨 진행바가 MY탭과 중복돼서 제거, 인사말+이름+스트릭을 한 줄로 축약(스트릭 문구도 "N일째"로 짧게). 배너는 히어로 카드 바로 아래(OT/클로징 모임 배너보다 위)에 배치
  - 브라우저 검증 완료: 배너 있을 때/없을 때 렌더링, admin CRUD(등록·활성전환·목록), 링크 클릭 시 새 탭 오픈
  - **루틴 딥링크 추가** (2026-07-17) — 처음엔 외부 URL만 가능해서 특정 루틴으로 못 연결되는 문제 발견. admin 배너 등록 화면에 "루틴 선택" 드롭다운 추가 → 고르면 연결 링크에 `routine:{id}` 자동 입력, 목록에도 루틴명으로 표시. 홈에서 배너 클릭 시 `routine:` 접두사면 외부 탭 대신 앱 안에서 `openRoutineDetail()`로 해당 루틴 상세를 바로 열어줌. 브라우저 검증 완료
  - **후속 보완 3종** (2026-07-17) — ①히어로 카드를 원래의 절반 이하로 축소(장식 원 축소, 인사말+이름+스트릭만 남기고 padding도 줄임, 이제 안 쓰는 `homeCertCount`/`homeCommCount` 참조 정리) ②admin 배너 등록에 이미지 URL 직접입력 외 **"🖼️ 이미지 업로드"** 버튼 추가 — `banner-images` 공개 버킷에 업로드 후 URL 자동 채움+미리보기 (마이그레이션 `[2026-07-17b]`, **실행 필요**) ③배너 목록에 **"수정"** 버튼 추가 — 클릭 시 폼에 기존 값 채워짐(이미지 미리보기 포함), "수정 완료"로 버튼 텍스트 전환, "수정 취소"로 되돌리기. 브라우저 검증 완료(폼 채우기·취소·이미지 미리보기 전부)
  - **이미지 없는 배너 기본 배경** (2026-07-17, 이후 수정) — 처음엔 브랜딩 3색을 한 그라데이션으로 섞었었는데, "합친 색 아니라 블루/그린/레드 각각 파스텔 하나씩"이라는 피드백으로 수정: `BANNER_PASTELS` 배열(블루 #E6F1FB·그린 #EAF3DE·레드/핑크 #FBEAF0, 기존에 앱 곳곳에서 쓰던 값 재사용)에서 배너 `id % 3`로 매 배너마다 단색 하나를 고정 배정. 이미지 없을 땐 스크림도 빼고 그 파스텔에 맞는 진한 텍스트 색(#185FA5/#3B6D11/#993556)을 써서 스크림 없이도 읽히게 함 — 이미지 있을 때만 어두운 스크림+흰 텍스트 유지
  - **루틴 이미지 자동 사용 + 오버레이 레이아웃** (2026-07-17) — ①배너에 이미지 URL이 없고 `routine:` 링크가 걸려있으면 그 루틴의 대표 이미지를 자동으로 가져다 씀(`_bannerImageUrl()`, 매번 최신 이미지로 조회하는 방식이라 루틴 사진이 바뀌면 배너도 자동 반영). admin 배너 등록에서 루틴 선택 시 "🌱 이 루틴의 대표 이미지가 자동으로 쓰여요" 힌트 노출(그 루틴에 이미지가 있을 때만) ②배너 레이아웃을 이미지+흰 텍스트박스 분리형에서, **이미지 위에 어두운 그라데이션 스크림 + 우측 하단에 제목/내용을 얹는 오버레이형**으로 변경 — 전체 높이가 150px 하나로 끝나서 훨씬 적게 차지함(기존엔 이미지 150px + 텍스트박스 + 점표시까지 총 220px 안팎). 순환 점 표시는 좌측 하단으로 이동. 브라우저 검증 완료(루틴 이미지 자동 반영, admin 힌트 노출조건, 오버레이 레이아웃)
  - **이미지 사이즈 통일 + 배너 자동 순환** (2026-07-17) — 루틴 카드 썸네일은 2단 그리드라 실제 폭이 좁아서(~175px, 비율 1.3:1) 나머지와 통일 불가능하다는 걸 확인, 대신 같은 폭(358px) 컨테이너를 쓰는 루틴 상세·루틴 등록 예시사진·홈 배너 셋을 전부 **높이 150px(비율 2.4:1)**로 통일(`.photo-upload` 160→150, 배너 100→150). 배너는 여러 개 등록하면 순서 1개만 보이던 걸 **4초마다 자동 순환**하도록 변경 — 하단에 현재 위치 점 표시, 클릭 시 지금 보이는 배너의 링크로 이동(`_bannerRotateIdx` 기준). 브라우저에서 순환·점표시·단일배너 시 정지 전부 확인
  - **공유 버튼을 아이콘 하나로 축소** (2026-07-21) — 청소년 루틴 상세의 "🔗 공유" 텍스트 pill이 다른 태그들과 줄바꿈되던 문제, 텍스트 빼고 🔗 아이콘만 있는 원형 버튼으로 바꿔서 한 줄에 들어가게 함(끗짱 쪽 액션 버튼 행은 라벨형 그대로 유지)

## 최근 완료 (2026-07-22)

- **커뮤니티 게시글 댓글 추가** — 지금까지 게시글(공지/후기)엔 좋아요만 있고 댓글이 없었음. `post_comments` 테이블 신규(cert_comments와 동일 패턴 — 본인/관리자만 삭제) + 댓글 시 게시글 작성자에게 푸시(`on_post_comment_push`, cert_comment_push와 동일 패턴). 카드에 "💬 댓글 N" 버튼 추가 → 누르면 기존 `routineDetailOverlay`(rdBody/rdCommentBar)를 재사용하는 `openPostDetail()`로 상세+댓글 목록+입력바 노출. 인증 댓글용이던 `currentCertDetailId`와 겹치지 않게 `currentPostDetailId`를 별도로 두고 `submitCommentFromBar()`에서 분기(기존 인증 댓글 로직은 안 건드림). 브라우저에서 댓글 등록·삭제·카운트 갱신 확인. 마이그레이션 `[2026-07-22]` 실행 완료
- **공지글 상단고정** — 커뮤니티 "전체" 목록에서 공지(notice)가 날짜순으로 후기들에 밀려 섞이던 걸, `_pinNotices()`로 공지를 항상 최상단(안정 정렬이라 그룹 내 최신순은 유지)으로 고정. 청소년 커뮤니티 탭·끗짱 리뷰 탭 양쪽에 적용. DB 변경 없음, 브라우저 검증 완료

## 최근 완료 (2026-07-21)

- **참여 인원 표시를 "N명 참여" → "👤 N/최대" 로 변경** — 청소년 루틴 상세 태그에서 `${r.people}명 참여`를 `👤 ${r.people}/${r.maxPeople}`로 교체. 브라우저 검증 완료
- **서류 승인·끗짱 가입 승인 알림 추가** — 루틴 참여 승인/거절은 이미 알림이 가고 있었는데(`on_participant_status_change`), 학교밖청소년 서류 승인과 끗짱 가입 승인엔 알림이 없던 걸 발견. `profiles` 테이블에 별도 AFTER UPDATE 트리거 2개 추가(`on_verify_status_notify`, `on_kkutjjang_status_notify`) — 기존 권한 가드 트리거(BEFORE UPDATE, `guard_verify_status`/`guard_kkutjjang_status`)와는 분리, 승인(approved)으로 바뀌는 순간만 `notify_push()`. 거절 알림은 요청 범위 밖이라 추가 안 함(기존처럼 인앱에서 사유만 확인). 마이그레이션 `[2026-07-21]` 실행 완료
  - **루틴 공유 링크(실제 URL) 추가** (2026-07-17) — `routine:` 배너 딥링크는 앱 안에서만 동작해서, 카톡·인스타 등 외부에 뿌릴 수 있는 진짜 URL이 없다는 문제 발견. 청소년·끗짱 루틴 상세(모집중/진행중 루틴만)에 **"🔗 공유"** 버튼 추가 → `?routine={id}` 쿼리 링크 생성, `navigator.share` 지원 시 공유 시트, 없으면 클립보드 복사(실패 시 프롬프트 폴백). 앱이 이 쿼리로 열리면 로그인 완료 후 자동으로 해당 루틴 상세를 띄우고 URL은 깨끗하게 정리(`pendingRoutineId`). 브라우저에서 딥링크 파싱→자동 오픈→URL 정리는 검증 완료, 클립보드/공유시트 자체는 헤드리스 환경 제약으로 실기기 확인 필요

## 최근 완료 (2026-07-22, 코드 리뷰로 발견한 보안 구멍 2건 수정)

코드 리뷰(Explore 에이전트)로 개선점을 훑다가 발견한 실제 보안 구멍 2건 처리:

1. **nadaeum(포인트)/role을 REST API로 직접 변조 가능했던 구멍** — `profiles_update` RLS 정책이 `auth.uid()=id`만 체크해서, 로그인 사용자가 Supabase 클라이언트로 직접 `profiles.update({nadaeum: 999999})`를 호출하면 그대로 통과됐음. 특히 댓글/후기 나다움은 애초에 클라이언트가 `earnNadaeum()`에서 `profiles.nadaeum`을 직접 update()하는 구조였어서, 실제 댓글/후기 작성 없이도 포인트를 무한정 올릴 수 있었음.
   - **DB 수정**: 인증(cert) 지급과 동일한 패턴으로 댓글(+1, 하루 5회 상한)·후기(+30, 루틴당 1회) 나다움도 서버 트리거(`award_comment_nadaeum` on cert_comments/post_comments, `award_review_nadaeum` on posts)가 실제 insert 건수 기준으로 지급하도록 변경. `guard_nadaeum_role` 트리거 신설 — nadaeum은 관리자이거나 트랜잭션 로컬 플래그(`_mark_nadaeum_trusted()`, 기존 지급/차감 함수들이 호출)를 거친 경우만 변경 허용, role은 관리자이거나 최초 가입 완료 이전(카카오 소셜 로그인 추가정보 입력 단계, 실명/생년월일/휴대폰이 아직 비어있는 시점)에만 1회 허용. 마이그레이션 `[2026-07-22d]`, **실행 필요**
   - **클라이언트 수정**: `earnNadaeum()`을 인증(submitCert)과 동일한 패턴으로 변경 — 직접 profiles.nadaeum을 쓰지 않고, 댓글/후기 insert 후 서버 값을 다시 조회해서 반영. 하루 댓글 5회 상한·후기 1회 제한을 추적하던 클라이언트 전용 상태(`nadaeumToday`, `nadaeumReviews`)는 서버가 실제 행 기준으로 판단하므로 제거
2. **localStorage에 실명/생년월일/휴대폰 평문 저장** — 로그인할 때마다 `hankkut_v4` 캐시에 PII가 그대로 직렬화됐음(공유 기기 위험). `persistState()` 헬퍼를 도입해 localStorage에 쓸 때만 realName/birthDate/phone을 제외하도록 수정(메모리상의 `state`는 그대로 유지, 앱 재실행 시 `loadProfileAndEnter()`가 어차피 서버에서 다시 채워주므로 기능엔 영향 없음). DB 변경 없음

`node --check`로 문법 검증 완료. 마이그레이션 `[2026-07-22d]` 실행 완료(2026-07-22). 실사용 테스트(댓글/후기/인증 나다움 지급, 카카오 신규가입 role 설정)는 아직 권장 사항으로 남아있음.

## 최근 완료 (2026-07-22, 배지/업적 컬렉션 + 끗짱 루틴 가시성 개선)

- **배지/업적 컬렉션 (MVP 6종)** — MY탭 인증 달력 아래에 배지 그리드 추가: 📸 첫 인증 / 🌱 첫 완주 / 🏆 완주 3회 / 🔥 연속 7일 / 🔥 연속 30일 / 💯 인증 50회. 나다움과 동일한 이유로 클라이언트가 조건을 스스로 판단하지 않고, `certifications`/`routine_participants` 실제 변화를 감지하는 서버 트리거(`award_cert_badges`, `award_completion_badges`)가 `user_badges` 테이블에 지급. 획득 순간 축하 푸시 발송. 마이그레이션 `[2026-07-22e]` 실행 완료. 브라우저에서 획득/미획득 렌더링 확인 완료(실사용 지급 테스트는 아직)
- **끗짱 루틴탭이 텅 비어 보이던 문제 + 신청 자격 서버 검증** — 끗짱 계정은 `_kMyRoutines()`(담당 루틴만 스코핑)를 홈/루틴탭/인증탭/통계 전부에 써서, 담당 루틴이 없으면 루틴탭 자체가 완전히 비어 보였음. 논의 끝에 **루틴탭(둘러보기)만** 전체 루틴이 보이도록 변경(`kFilterRoutine()`이 `state.routines` 사용), 홈/인증탭/통계는 기존처럼 담당 루틴만 스코핑 유지. 내 담당 루틴엔 파란 "내 담당" 배지 표시. 부수적으로 발견한 구멍도 같이 수정: 학교밖 전용 루틴이어도 모두에게 보이지만(기존부터 이미 그랬음), 참여 신청 자격(끗짱은 신청 불가, 학교밖 전용은 서류 승인 필요)이 클라이언트 UI만 막고 있어서 REST API 직접 호출로 우회 가능했던 걸 `guard_participant_apply` 트리거로 서버에서도 막음. 마이그레이션 `[2026-07-22f]` 실행 완료. 브라우저에서 전체 노출·내 담당 배지·학교밖 전용 배지 렌더링 확인 완료
  - **알려진 한계**: 루틴탭에서 다른 끗짱 담당 루틴 카드를 눌러도 기존 관리 화면(승인/콕찌르기 버튼 포함)이 그대로 열림 — 실제 액션은 RLS가 막지만("권한 없음" 에러), 버튼 자체를 숨기진 않음. 필요시 추가 작업 가능

## 최근 완료 (2026-07-23, QA 이슈트래커 4~9번 대응)

스프레드시트 QA 이슈트래커에서 이어서 6건 처리(1~3번은 04bab32 커밋에서 이미 처리):

4. **회원가입 생년월일 입력박스가 다른 필드보다 가로로 길게 보임** — `.auth-form`이 flex column인데, 네이티브 date input이 세그먼트+달력아이콘 때문에 갖는 내재적(intrinsic) 최소 폭이 flex 기본 동작(`min-width:auto`)과 맞물려 형제 필드보다 넓게 삐져나왔음. `.date-field-wrap`과 input 양쪽에 `min-width:0`을 줘서 flex 부모 폭에 맞게 강제로 줄어들게 함
5. **모바일 화면 양쪽에 여백** — 이전(2026-07-22) 수정이 `max-width:390px` 이하에서만 여백을 없앴는데, 그 기준이 구형 iPhone 기준이라 iPhone 14/15/16(393px)·Pro Max(430px)·흔한 안드로이드(360~412px) 등 요즘 폰 대부분을 못 덮었음. 기준을 480px로 올려서 실제 폰 대부분을 커버(데스크톱 "폰 프레임" 모양은 그대로 유지)
6. **뒤로가기 시 원래 있던 페이지로 랜딩** — 1번(오버레이/시트 뒤로가기) 수정 때 만든 히스토리 스택을 탭 전환까지 확장. `switchTab`/`kSwitchTab`이 탭을 바꿀 때마다 히스토리를 쌓아서, 뒤로가기를 누르면 방문했던 탭을 역순으로 되짚어감(홈→루틴→커뮤니티 이동 후 뒤로가기 두 번이면 루틴→홈). 오버레이가 열려있으면 그것부터 닫고, 다 닫힌 뒤에야 탭이 바뀜(같은 스택 공유)
7. **커뮤니티 공지/후기 클릭 안 됨(댓글 버튼만 동작)** — `_postCard()`가 카드 전체엔 클릭 핸들러가 없고 "댓글" pill에만 `openPostDetail()`이 걸려있던 게 원인. 카드 전체에 `onclick`을 걸고, 좋아요·삭제 버튼엔 `event.stopPropagation()`을 추가해 카드 클릭과 안 겹치게 분리
8. **게시글 수정 기능 추가** — 카드/상세화면에 연필 아이콘 추가(삭제와 동일 권한: 본인 또는 관리자). 기존 글쓰기 시트(후기는 청소년용, 공지는 끗짱용)를 기존 내용으로 채워 재사용, `editingPostId`로 insert/update 분기. `posts` 테이블에 UPDATE RLS 정책 자체가 없었어서 신규 추가 + 작성자/글종류/연결루틴은 못 바꾸게 가드(`guard_post_update`). 마이그레이션 `[2026-07-23]` 실행 완료
9. **게시글 삭제 안 됨** — 두 가지 원인 수정: ①Supabase는 RLS에 막히면 에러 없이 "0건 삭제"로 조용히 끝나서 실제로 안 지워져도 앱은 성공한 것처럼 넘어갔음 — `.select('id')`로 실제 삭제 행을 받아 0건이면 명시적 알림 ②상세화면에서 삭제하면 뒤 목록은 갱신되지만 보고 있던 상세화면 자체가 안 닫혀서 "안 지워지는 것처럼" 보였음 — 삭제 성공 시 상세화면도 자동으로 닫히게 수정

`node --check`로 문법 검증 + 브라우저에서 각 항목 시나리오 확인 완료(4,5,7,8,9는 실제 클릭/렌더링까지, 6은 history API 시뮬레이션). **남은 것**: 화면 위/아래로 잘려서 아직 못 본 QA 항목 2건 확인 필요.

## 최근 완료 (2026-07-23, QA 이슈트래커 2차 배치 6건 + 완주화면 겹침)

새 QA 스프레드시트 배치 6건 처리:

1. **루틴탭 "전체" 카테고리가 헷갈림** — 청소년 탭은 "전체" 삭제하고 기본값을 "참여 중"으로. 끗짱 탭은 "전체"(다른 담당자 루틴까지 다 보기, 지난 세션에 의도적으로 추가한 기능)는 남기되 기본값만 "진행 중"(내 담당만)으로 변경. 이 과정에서 `kFilterRoutine()`이 전체 필터 타입에 대해 org-wide `state.routines`를 쓰던 버그도 같이 발견해 수정(전체 탭만 org-wide, 나머지는 `_kMyRoutines()`로 원복)
2. **홈 배너 캐러셀 내용 안 보임/클릭 오류** — 정확한 원인 재현은 못 했지만, 배너 이미지 URL이 깨져있으면(만료·삭제 등) 흰 배경에 흰 글씨가 겹쳐 안 보이는 상태가 되는 실제 버그를 발견해 수정 — 이미지 로드 실패 시 파스텔 배경+진한 텍스트로 자동 폴백(`_failedBannerImages` Set으로 추적). 클릭 핸들러 자체의 에러는 재현 못 함(추가 정보 필요시 재문의)
3. **카카오 로그인 시 실명 자동 채움** — 추가정보 입력 화면에서 닉네임만 카카오 이름으로 채워지고 실명(cpRealName)은 비어있던 것을, 실명도 같이 채우도록 수정(둘 다 수정 가능)
4. **MY탭 "완주한 루틴" 카운트가 신청 전인데도 1로 표시** — `state.youth.completeCount`가 데모용 하드코딩 값(1)으로 초기화된 뒤 어디서도 재계산되지 않던 버그. 실제 "완주한 루틴" 목록 계산 로직(내가 참여자이고 status='done')을 재사용해 카운트하도록 수정, 안 쓰는 `completeCount` 필드 제거
5. **"지금 찍기" 워터마크가 안 보임** — 워터마크 자체는 사진에 정상적으로 새겨지고 있었음(테스트로 확인). 촬영 직후 뜨는 110px 남짓 미리보기 타일에서 원본 대비 너무 작아져 육안으로 안 보이던 게 원인 — 사진 자체 워터마크 크기를 바꾸는 대신 미리보기 타일 위에 "📸 실시간" UI 뱃지를 얹어 확인 가능하게 함 (`certPhotoWatermarked` 배열로 카메라/갤러리 출처 추적)
6. **인증글 상세화면에서 사진 클릭 안 됨** — 사진 `<img>`에 클릭 핸들러 자체가 없었음. 클릭 시 전체화면 라이트박스로 원본 비율 크게 보이도록 추가(`photoLightboxBg`, X버튼/배경탭/뒤로가기 스와이프로 닫힘 — 기존 openSheet/closeSheet 재사용이라 히스토리 스택에 자동 통합)

추가로 완주 축하 화면의 배지 라벨이 이모지와 겹치던 문제도 별도 수정(28588d8 커밋).

브라우저에서 전부 시나리오 검증 완료(2번 제외, 실제 프로덕션 데이터로 재현 못 함). DB 변경 없음.

## 최근 완료 (2026-07-23, 루틴 피드 전체 기간 누적으로 변경)

- **완주한 루틴 클릭해도 그동안 인증글을 볼 수 없던 문제 발견 → 해결** — 사용자 문의로 확인해보니 두 가지가 겹쳐있었음: ①MY탭 "완주한 루틴" 목록 항목에 클릭 핸들러 자체가 없었음 ②설령 클릭되게 해도 `openRoutineFeed()`가 "최근 24시간" 인증만 보여주도록 설계돼 있어서(원래 "오늘 누가 인증했는지" 보는 용도), 완주한 지 며칠 지난 루틴은 항상 텅 빈 화면만 떴을 것
  - `openRoutineFeed()`의 24시간 필터를 제거해 그 루틴의 전체 기간 누적 인증글을 다 보여주도록 변경 — 진행중·완료 루틴 둘 다 적용
  - 완주한 루틴은 상단에 기존 `openRoutineDetail()`의 완주 축하 문구+후기/편지/공유 버튼을 그대로 가져와서 인증 기록 위에 얹음, "오늘 인증하기" 플로팅 버튼은 완주 루틴에서 자연스럽게 숨김
  - 루틴탭 카드 클릭 라우팅(`(isActive||isDone)&&isMine`)과 MY탭 "완주한 루틴" 리스트 둘 다 `openRoutineFeed()`로 연결
  - 브라우저에서 진행중/완료 두 케이스 모두 확인(전체 기간 인증글 노출, 완주 액션 버튼, 인증 버튼 숨김). DB 변경 없음

## 최근 완료 (2026-07-24, QA 이슈트래커 3차 배치 1~9번)

새 QA 스프레드시트 배치 9건 전부 처리:

1. **루틴 모집기간/진행기간 분리** — `start_date` 하나가 "모집 마감"과 "진행 시작"을 동시에 나타내던 걸 분리. `routines.recruit_end_date` 컬럼 신설, 루틴 생성/수정 폼에 "모집 마감일" 필드 추가(모집 마감일 ≤ 진행 시작일 검증), 카드 D-day·상세화면 태그 모두 새 필드 기준으로 표시(청소년/끗짱 둘 다). 기존 루틴은 NULL → 시작일로 폴백해 기존 동작 유지
2. **게시글 줄바꿈/공백 안 반영** — 게시글·인증글·댓글 렌더링에 `white-space:pre-wrap` 누락. 게시글 카드/상세, 인증글 상세, 댓글 전부에 추가
3. **전체 공지가 미리보기 없이 다 보임** — 공지(notice) 타입만 3줄 클램프 + "더보기" 추가(후기는 그대로 전체 노출)
4. **리워드 이미지 URL 깨지면 나다움스토어에서 깨진 아이콘 노출** — `onerror`로 🎁 이모지 폴백 추가(배너와 동일 패턴)
5. **가입일이 2024년으로 고정 표시** — `completeCount`와 동일한 패턴의 버그: `joinDate`가 데모 하드코딩 값에서 갱신되는 로직이 아예 없었음. `profiles.created_at` 기준으로 계산하도록 수정
6. **인증글 작성칸 문구 정리** — "오늘 어땠어요?" 라벨 삭제, placeholder를 "내용을 입력해 주세요."로 변경(15자 요구사항은 아래 힌트에 이미 별도 표시 중이라 중복 제거)
7. **알림 클릭해도 MY탭으로 이동 안 함** — `notify_push()`가 모든 알림에 같은 루트 URL만 써서 클릭해도 그냥 앱만 열렸음. URL에 `?tab=my` 추가 + 클라이언트가 로그인 후 이 쿼리를 읽어 자동으로 MY탭 전환(화이트리스트로 안전 처리) + 서비스워커가 이미 열린 창도 새 URL로 navigate하도록 수정(기존엔 focus만 하고 URL 무시)
8. **참여 이력 없어도 후기 작성 시 완료 루틴 전부가 선택 목록에 뜸** — `openCommWriteWithRoutine()`의 완료 루틴 필터가 org 전체 `status==='done'`만 보고 참여 여부를 체크 안 했음. 내가 실제 참여자인 루틴만 뜨도록 필터 추가

9. **폐강 루틴 신청자 목록에서 취소해도 재접속 시 그대로 남아있음** — 정확한 원인 재현은 못 했음(RLS 정책 자체는 상태와 무관하게 담당 끗짱/관리자에게 항상 열려있어 코드상 명확한 차단 지점을 못 찾음). 다만 Supabase가 RLS 불일치 시 에러 없이 "0건 반영"으로 조용히 끝나버리는 걸 이번 세션에서 여러 번 발견한 터라, 승인/거절/승인취소/폐강처리 네 함수 전부에 `.select()`로 실제 반영 행을 확인하는 방어 코드 추가 — 0건이면 명확한 실패 알림 노출. 이후에도 재현되면 이 알림이 뜨는지로 원인 범위를 좁힐 수 있음

DB 마이그레이션 `[2026-07-24]`(recruit_end_date), `[2026-07-24b]`(notify_push URL) 실행 완료. 브라우저에서 각 항목 시나리오 검증 완료.

## 최근 완료 (2026-07-24, 모집 시작일 추가 + 모집 기간 신청 차단)

QA 배치 이후 사용자 추가 요청 — "모집 마감일"만 있고 시작일이 없어서 언제부터 신청받는지 표현이 안 됐던 것 + 실제로 모집 기간(시작~마감) 밖에서는 신청 자체가 안 되게 해달라는 요청.

- `routines.recruit_start_date` 컬럼 신설. 생성/수정 폼의 "모집 마감일" 단일 필드를 "모집 기간"(시작일+마감일)으로 변경(진행 기간과 동일한 형태), 검증 추가(모집 시작일 ≤ 모집 마감일 ≤ 진행 시작일)
- **신청 차단을 서버(`guard_participant_apply` 트리거)와 클라이언트 양쪽에 구현** — 모집 시작 전이거나 마감 후면 신청 자체가 막힘. 루틴 상세화면에서도 "참여 신청하기" 버튼 대신 "아직 모집 시작 전이에요"/"모집이 마감됐어요" 안내로 교체
- 기존 루틴은 두 필드 다 NULL이라 이 검사를 건너뛰어(기존 동작 그대로 유지), 새로 만드는 루틴부터만 실제 적용됨(폼에서 필수로 받기 시작했으므로)
- 마이그레이션 `[2026-07-24c]` 실행 완료. 브라우저에서 모집 전/정상/마감 후 3가지 케이스 전부 확인

## 최근 완료 (2026-07-24, 관리자 편의 기능 2건)

지금까지 루틴 생성은 index.html(모바일 앱)에서 끗짱 화면으로 전환해야만 가능했는데, 실제로는 유스보이스 직원(관리자)이 대부분 루틴을 만들다 보니 매번 앱으로 전환하는 게 불편하다는 요청에서 시작.

1. **admin.html "루틴" 탭에 등록/수정 폼 추가** — index.html의 "루틴 만들기"와 **완전히 동일한 필드·검증**(이모지, 담당 끗짱 선택, 참여대상, 모집기간/진행기간, 최대·최소 인원, 온라인 모임(줌), 인증 예시 사진 등). 사진은 배너 등록과 같은 방식으로 `routine-covers` 버킷 업로드. 루틴 목록에 "수정" 버튼도 추가(기존엔 폐강/보관/삭제만 있었음). `loadData()`의 routines 조회를 `select('*')`로 넓혀서 수정 시 기존 값이 프리필되도록 함. DB/RLS 변경 없음(기존 정책이 `is_admin()`을 이미 허용). 브라우저에서 필드 검증 순서·수정 모드 프리필 확인 완료, 실제 로그인 후 사진 업로드 포함 전체 흐름은 실기기 확인 권장
2. **수동 포인트 지급에 참가자 검색 추가** — "청소년 선택" select 드롭다운이 인원 많아지면 찾기 불편했음. 닉네임/실명/휴대폰번호로 검색되는 자동완성 콤보박스로 교체(최대 30명 매칭, 클릭 선택). 지급 로직(`grantNadaeum()`)은 hidden input의 ID값을 그대로 써서 변경 없음. DB 변경 없음

## 최근 완료 (2026-07-24~25, 청소년 홈 탭 제거 → 루틴 탭 통합, 인증 탭 정리)

챌린저스 등 참고 앱 벤치마킹 논의 끝에 "참여 현황은 인증 탭이 전담, 나머지 탭은 탐색용"으로 역할을 정리하기로 함. 청소년 쪽부터 적용.

1. **청소년 5탭 → 4탭** — 홈 탭을 없애고 홍보 배너·오늘의 미션·모임 알림·알림 유도 배너를 루틴 탭 상단으로 이동, 루틴 탭을 "홈"으로 이름 변경(아이콘도 `ti-home`으로 교체)하고 기본 진입 탭으로 삼음. 홈의 "참여 중인 루틴" 전체 리스트 섹션은 삭제(인증 탭이 그 역할을 전담). `switchTab`/뒤로가기(popstate)/딥링크 검증 목록 등 `'home'` 탭을 참조하던 곳을 모두 `'routine'` 기준으로 수정 — 안 고쳤으면 뒤로가기·공유링크 진입 시 청소년 계정에서 에러 났을 부분
2. **루틴(홈) 탭 정렬/필터 정리** — 칩을 `전체(기본)/모집 중/완료`로 통일(참여중·진행중 칩 제거, 끗짱 쪽도 동일하게 변경). 정렬 기준을 "모집 중 → 내가 참여 중인 것 → 나머지" 순으로 변경
3. **"오늘의 한끗" 미션 카드 축소** — 루틴별 인증하기 목록을 보여주던 카드가 인증 탭과 완전히 중복돼서, 한 줄짜리 "오늘 미인증 루틴 N개 남았어요 → 인증하러 가기" 배너로 축소(클릭 시 인증 탭 이동)
4. **인증 탭 빈 상태 개선 + 모임 알림 이동** — 빈 상태를 "🧭 아직 참여 중인 루틴이 없어요 + 루틴 둘러보기"로 교체(클릭 시 루틴 탭의 모집 중 필터로 바로 이동). OT/클로징 모임 줌 알림 배너를 홈에서 인증 탭으로 옮김(인증하러 가는 맥락에 더 맞음)
5. **인증 탭 아이콘에 할 일 뱃지 추가** — 미인증 루틴 수 + 오늘 모임 수를 더해 빨간 숫자 뱃지로 표시(9개 넘으면 "9+"), 하단 네비 인증 버튼도 튀어나온 원형 스타일에서 다른 탭과 동일한 평범한 스타일로 변경(청소년/끗짱 공유 CSS라 양쪽 다 적용됨)
6. **끗짱 홈의 "오늘 인증 현황" 중복 섹션 제거** — 인증 탭(`kCertTabList`)과 완전히 동일한 내용이라 홈 쪽만 제거. 끗짱은 홈의 승인 대기·통계 카드가 인증/루틴 탭엔 없는 고유 정보라 5탭 구조는 그대로 유지하기로 결정(4탭 통합은 보류)

브라우저에 남아있던 실제 로그인 세션(관리자 계정, `loginType='both'`)으로 청소년/끗짱 역할 전환하며 실기기 수준 검증 완료 — 양쪽 탭 전환, 필터 정렬(모집중 우선), 배너 75px 높이에서 실제 텍스트 가독성, 인증 탭 빈 상태→루틴 둘러보기→모집중 필터 자동 선택 흐름, 브라우저 뒤로가기까지 전부 콘솔 에러 없이 확인. DB 변경 없음.

## 최근 완료 (2026-07-25, 자유루틴(상시 모집) + 루틴별 완주 인정 비율)

챌린저스 등을 보며 "포인트 걸린 루틴 말고 상시로 누구나 참여할 수 있는 루틴도 있으면 좋겠다"는 아이디어에서 시작. 계획 문서(`.claude/plans/crystalline-kindling-flute.md`)에 설계 근거를 남겨둠.

- **자유루틴 타입 추가** — `routines.is_free`(신규). 모집은 상시로 열려있고(마감 없음) 나다움 포인트는 절대 지급 안 됨. 진행기간은 참가자별 개인 시작일이 아니라 담당 끗짱이 정하는 고정 캘린더 구간(예: 100일) — 참여 승인은 지금처럼 끗짱이 직접 함(자동승인 아님)
- **핵심 함정 발견 및 해결**: `status`가 'recruit'→'active'로 바뀌는 순간(첫 승인 시) 청소년 쪽 신청 UI 전체가 사라지고 모집중 필터/카드에서도 빠져버리는 기존 구조 때문에, 그냥 모집마감일만 비워두는 걸로는 "상시 모집"이 전혀 구현되지 않음을 확인. `_isFreeOpen(r)` 헬퍼(끝난 상태 아니고 진행종료일도 안 지남)를 만들어 `openRoutineDetail`/`openKRoutineDetail`의 신청 게이트, `filterRoutine`/`kFilterRoutine`의 모집중 필터, 카드 정렬·배지("상시 모집" 칩)에 전부 반영
- **완주 인정 비율 설정** — 기존 전역 하드코딩 2/3을 `routines.completion_ratio_pct`(기본 67, 기존 루틴과 동일 계산 결과)로 루틴마다 설정 가능하게 함(일반 루틴만, 비율만 — 인증/댓글/후기 포인트 액수 10/1/30은 전역 그대로). 겸사겸사 `openKRoutineFeed`가 실제 루틴 기간과 무관하게 21일로 하드코딩돼있던 버그도 같이 고침(`openKReportCard`처럼 실제 기간 기준으로 통일)
- **DB**: 마이그레이션 `[2026-07-25]` — `is_free`/`completion_ratio_pct` 컬럼 추가, `on_cert_nadaeum_payout()`(자유루틴 지급 스킵 + 루틴별 비율 적용), `guard_participant_apply()`(자유루틴은 상태·진행종료일 기준으로 신청 차단하도록 재작성) 갱신
- **index.html + admin.html 양쪽 루틴 생성/수정 폼**에 "루틴 타입"(고정기간/자유루틴) 토글과 "완주 인정 비율" 입력 추가, 자유루틴 선택 시 모집기간·비율 필드 숨김. admin.html 루틴 목록·모니터링 CSV에도 자유루틴 표시/제외 반영
- **마이그레이션 `[2026-07-25]` 실행 완료** — Supabase SQL Editor에서 사용자가 직접 실행. 실행 후 실제 DB에 `is_free:true`인 자유루틴을 하나 만들어 저장 확인 후 즉시 삭제하는 방식으로 종단 검증함(정상 저장 확인)
- **남은 것**: ①승인 후에도 다른 사람 신청 가능한지 ②종료일 지나면 신청 막히고 done 전환되는지 ③일반 루틴 비율 커스텀 시 실제 지급 기준 반영되는지는 실사용 데이터로 추가 확인 권장

브라우저에 남아있던 실제 관리자 세션으로 index.html·admin.html 양쪽에서 루틴 타입 토글 UI 동작(필드 숨김/표시, 기존 루틴 수정 시 프리필)까지 확인, 콘솔 에러 없음.

## 최근 완료 (2026-07-25, 랜딩페이지 후원 CTA + 비로그인 전체 조회 차단 보안 수정)

후원 유도 방법을 고민하다가 랜딩페이지(비로그인 방문자용)에 후원 CTA가 아예 없다는 걸 발견해서 추가. 그 다음 "청소년 후기를 랜딩페이지에 실시간 노출" 아이디어를 설계하던 중 **더 급한 보안 문제를 발견**해서 먼저 처리함.

- **랜딩페이지 후원 CTA 3곳 추가** — 상단 네비(항상 노출), 하단 CTA 섹션, 푸터. 외부 후원 페이지(secure.donus.org)로 새 탭 연결. 끗짱 MY 탭에도 동일 링크로 "후원하기" 메뉴 추가(index.html)
- **보안 긴급 수정 — 비로그인 전체 테이블 조회 차단**: `profiles`/`routines`/`routine_participants`/`certifications`/`cert_likes`/`cert_comments`/`posts`/`post_likes`/`post_comments` 총 9개 테이블의 SELECT RLS 정책이 전부 `USING (true)`였음 — 로그인 여부와 무관하게 anon key만으로 전체 조회가 가능한 상태였음. `profiles`엔 `real_name`/`birth_date`/`phone`/`region_sido·sigugun`/`verify_doc_url`까지 있어서 **미성년자 개인정보가 사실상 공개돼있던 것**. 전부 `auth.uid() IS NOT NULL`(로그인 사용자만) 조건으로 교체(마이그레이션 `[2026-07-25b]`). `rewards`(리워드 카탈로그)는 민감정보 없어 공개 유지. 앱은 항상 로그인 후에만 이 테이블들을 조회하므로 기존 기능엔 영향 없음
- **청소년 후기 공개 노출 기능은 보류** — "실시간 자동 노출"은 로그인 사용자 대상으로 쓰인 후기를 비로그인 공개 페이지에 그대로 내보내는 셈이라 동의 범위를 벗어남 + 필터링 없이 부적절한 내용이 노출될 위험. "관리자가 고른 후기만, 익명화해서 노출"하는 방향으로 다음에 별도 진행하기로 함
- **마이그레이션 `[2026-07-25b]` 실행 완료** — Supabase SQL Editor에서 사용자가 직접 실행. anon key로 `profiles`/`posts`/`certifications`/`routine_participants`에 직접 REST 요청 날려서 전부 빈 배열 반환하는 것 확인(차단 검증 완료), 로그인 상태에서 홈/커뮤니티 탭도 정상 동작 확인

## 최근 완료 (2026-07-25, 루틴별 인증 지급 포인트 설정)

완주 인정 비율에 이어, 인증 1회당 지급되는 나다움(기존 전역 10 하드코딩)도 루틴마다 다르게 설정 가능하게 함.

- **`routines.cert_point_amount`**(기본 10, 1~1000) 추가, `on_cert_nadaeum_payout()` 트리거의 하드코딩된 `10`을 전부 이 값으로 교체(마이그레이션 `[2026-07-25c]`)
- **범위는 인증 포인트만** — 댓글(+1)/후기(+30)는 하나의 트리거 함수(`award_comment_nadaeum`)를 `cert_comments`/`post_comments`가 공유하고, 공지처럼 특정 루틴에 안 묶이는 댓글도 있어서 이번엔 제외하고 전역 고정 유지하기로 결정
- **index.html + admin.html** 루틴 생성/수정 폼에 "인증당 지급 포인트" 입력 추가(완주비율 필드와 동일 패턴, 자유루틴 선택 시 같이 숨김). 인증 완료 모달의 "+10 나다움" 하드코딩 표시도 실제 루틴 값을 반영하도록 수정
- 브라우저에서 자유루틴 토글 시 완주비율·포인트 필드가 함께 숨겨지는 것 확인, 콘솔 에러 없음
- **마이그레이션 `[2026-07-25c]` 실행 완료** — Supabase SQL Editor에서 사용자가 직접 실행, `cert_point_amount` 컬럼 확인 완료

## 최근 완료 (2026-07-25, 커뮤니티 글 카드 압축 + 후기 제목 자동화)

커뮤니티 글이 길면 카드가 너무 커 보인다는 피드백에서 시작. 논의 끝에 두 가지로 정리:

- **공지·후기 본문 미리보기 3줄 → 2줄로 통일** — 기존엔 공지만 3줄 클램프+"더보기"가 있었고 후기는 전체 노출이라 후기 글이 길면 카드가 커졌음. 이제 둘 다 2줄 클램프 + "더보기"로 통일(`_postCard`, index.html)
- **후기 글 제목을 루틴명으로 자동 표시** — 후기는 어차피 참여한 루틴에 대해서만 쓸 수 있어서, 사용자가 직접 입력하던 "제목" 칸을 없애고 연결된 루틴의 현재 이름을 `"{이모지} {루틴명} 후기"` 형태로 항상 자동 표시하도록 변경(`_postDisplayTitle()` 헬퍼, `loadPosts()`에 `routineId` 매핑 추가). DB엔 원래 제목 없이 저장되고(`posts.title`은 nullable이라 마이그레이션 불필요), 화면에서 항상 루틴명 기준으로 동적 계산 — 루틴 이름이 나중에 바뀌어도 후기 제목이 최신으로 따라감. 공지는 기존처럼 직접 입력한 제목 그대로 유지
- 브라우저에서 기존 후기(더미 제목 "테스트"/"후기") 다시 열어보니 실제 연결된 루틴명으로 정확히 표시되는 것, 수정 화면에도 제목 칸 없이 루틴명만 컨텍스트로 보이는 것 확인. 콘솔 에러 없음. DB 변경 없음

## 최근 완료 (2026-07-25, 후기 글 사진 첨부)

후기 작성 시 사진 1장을 선택적으로 첨부할 수 있게 함(인증글처럼 여러 장이 아니라 가볍게 1장만 하기로 결정).

- **`posts.photo_url`** 컬럼 추가(마이그레이션 `[2026-07-25d]`). 인증글과 같은 `cert-photos` 스토리지 버킷을 재사용(워터마크는 후기 특성상 불필요해 미적용)
- 후기 쓰기 시트에 단일 사진 업로드 UI 추가(루틴 만들기 폼의 대표사진 업로드와 동일한 `.photo-upload` 패턴 재사용). 수정 시 기존 사진 프리필, 안 바꾸면 유지
- 커뮤니티 피드 카드엔 56×56 작은 썸네일(카드 압축 기조 유지), 상세화면엔 큰 이미지로 표시
- 브라우저에서 사진 업로드 UI가 수정 시트에 정상 렌더링되는 것 확인, 콘솔 에러 없음
- **마이그레이션 `[2026-07-25d]` 실행 완료** — Supabase SQL Editor에서 사용자가 직접 실행, `photo_url` 컬럼 확인 완료

## 최근 완료 (2026-07-25, 자유루틴을 참여자별 개인 기간(5/15/30/66/100일)으로 전환)

앞서 만든 자유루틴은 담당 끗짱이 정한 하나의 고정 캘린더 구간을 모든 참여자가 공유하는 모델이었는데, 사용자가 원래 그리던 그림은 참여자 각자가 신청할 때 자기 기간을 고르고 승인 시점부터 개인적으로 시작되는 모델이었음이 뒤늦게 확인됨. Plan 모드로 전면 재설계.

- **데이터 모델**: `routine_participants`에 `duration_days`(5/15/30/66/100만 허용, CHECK 제약), `approved_at`(승인 시점 스탬프) 추가(마이그레이션 `[2026-07-25f]`). 자유루틴의 `routines.start_date`/`end_date`는 이제 항상 `null`(진행기간 필드 자체가 없어짐 — 참여자마다 다르므로)
- **`_personalPeriod(r, p)` 헬퍼** 신설 — 루틴+참여자 쌍에서 개인 시작/종료일과 완료 여부를 계산하는 단일 지점. 일반 루틴은 기존 루틴 공유 기간을 그대로 반환해 동작 변화 없음
- **신청 시 기간 선택** — `applyRoutineOverlay`에 5/15/30/66/100일 선택 칩 추가(자유루틴일 때만 노출), `guard_participant_apply()` 트리거가 `duration_days` 필수/허용값을 검증하도록 재작성(기존 `end_date` 기반 체크는 자유루틴엔 더 이상 없어서 제거)
- **완료 판정을 참여자 단위로 분리** — 자유루틴은 `routine.status`가 절대 `done`이 안 되므로(참여자마다 종료일이 다름), `_myActiveRoutines()`/MY탭 완주목록/완주 축하 팝업(`_maybeCelebrateCompletion`)/완주 리포트(`openReportCard`)/완주 축하 화면(`openCompleteSuccess`)을 전부 `_personalPeriod`의 개인 완료 여부 기준으로 갈아끼움. 새 공용 헬퍼 `_myCompletedRoutines()`로 통일
- **루틴 카드**: 자유루틴 + 본인 참여 카드는 공유 진행률 바 대신 개인 종료일 기준 D-day 표시, 개인 기간이 끝나면 그 사람 카드만 "완주" 스타일로 바뀜(다른 참여자·비참여자 화면엔 영향 없음 — 클라이언트에서 매번 현재 로그인 사용자 기준으로 계산하므로)
- **생성/수정 폼**: 자유루틴 선택 시 진행기간 입력 필드도 완주비율·포인트·모집기간과 함께 숨김(index.html + admin.html 양쪽), `_isFreeOpen()`을 상태만 보도록 단순화(더 이상 routine.endDate 볼 필요 없음 — 자유루틴은 루틴 레벨에서 절대 끝나지 않음)
- **명시적으로 범위 제외**: 끗짱 완주 리포트의 참여자 평균 달성률(자유루틴은 참여자마다 기간이 달라 "평균"이 약함), admin.html CSV 모니터링의 자유루틴 참여자별 기간 표시 — 필요해지면 후속 요청으로
- 브라우저에서 확인: ①자유루틴 선택 시 진행기간까지 포함해 모든 관련 필드 숨김 ②신청 오버레이에 기간 선택 칩이 뜨고 선택 상태 토글되는 것(mock 데이터로 검증, 실제 자유루틴 신청까지는 DB 마이그레이션 후 확인 필요) — 콘솔 에러 없음
- **남은 것 — 사용자가 직접 실행 필요**: 마이그레이션 `[2026-07-25f]`를 Supabase SQL Editor에서 실행해야 실제 신청이 동작함(컬럼 없어도 기존 기능은 안전). 실행 후 ①자유루틴 신청 시 기간 선택 강제되는지 ②승인 시 approved_at 찍히는지 ③개인 기간(짧게 5일로 테스트) 지나면 그 사람만 완주 처리되고 MY탭·완주축하가 뜨는지 실사용 확인 권장

## 최근 완료 (2026-07-25, 자유루틴 실사용 검증 중 발견한 버그 2건)

마이그레이션 `[2026-07-25f]` 실행 후 실제 테스트 루틴으로 신청→승인→완주 전체 흐름을 검증하는 과정에서 발견·수정:

1. **개인 종료일 계산 타임존 버그** (커밋 `0dc06c3`) — `_personalPeriod()`가 ms 덧셈 후 `toISOString()`으로 종료일을 뽑아서 UTC+9 환경에서 하루 밀리는 버그(5일 참여인데 종료일이 하루 당겨짐). `setDate()`로 로컬 날짜 필드를 직접 연산하도록 수정
2. **자유루틴 인증 시 나다움이 쌓이는 것처럼 보이던 모달 문구** — 실제 잔액은 안 늘어남(서버 트리거 `on_cert_nadaeum_payout`는 `is_free`면 처음부터 지급을 건너뜀, 정상 동작 확인). 문제는 클라이언트 `submitCert()`가 자유루틴 여부를 안 가리고, 서버가 지급 안 했을 때(자유루틴은 항상 이 경우) 무조건 "+N 나다움 쌓이고 있어요"를 보여줬던 것 — 절대 지급되지 않을 걸 쌓이는 것처럼 오해하게 만든 화면 버그. 인증 완료 모달에서 `certR.isFree`면 나다움 배지 자체를 안 띄우도록 수정(index.html:3144-3160). DB 변경 없음

`node --check`로 문법 검증 완료. 브라우저 실기기 검증은 다음 세션에서 이어서.

## 최근 완료 (2026-07-25, 루틴 상단 고정)

특정 루틴을 청소년/끗짱 루틴탭 목록 맨 위에 고정 노출하고 싶다는 요청.

- **`routines.pinned`**(기본 false) 추가(마이그레이션 `[2026-07-25g]`). 별도 RLS 불필요 — 기존 `routines_update` 정책(담당 끗짱 `led_by` 또는 관리자)이 이미 모든 컬럼에 적용되므로 그대로 재사용
- **정렬 우선순위**: 고정된 루틴이 최우선, 그 다음 기존 기준(청소년: 모집중→내가 참여 중→나머지 / 끗짱: 모집중→나머지) 그대로 유지. 카드 제목 앞에 "📌" 표시(청소년 루틴탭 `renderRoutineGrid`, 끗짱 루틴탭 `kFilterRoutine`, admin.html 루틴 테이블 전부 반영)
- **생성/수정 폼에 "📌 루틴 목록 상단에 고정" 체크박스 추가** — index.html(끗짱 폼) + admin.html 양쪽, 완주비율/포인트 필드와 달리 루틴 타입(고정기간/자유루틴)과 무관하게 항상 노출(자유루틴도 고정 가능)
- 브라우저에서 폼 렌더링(체크박스 위치·문구) 확인, 콘솔 에러 없음
- **마이그레이션 `[2026-07-25g]` 실행 완료** — Supabase SQL Editor에서 사용자가 직접 실행, anon 키로 `select=id,pinned` 요청해 컬럼 존재 읽기전용 검증 완료(RLS로 빈 배열 반환은 정상). 실제 체크→저장→목록 최상단 노출은 다음 세션에서 로그인 후 확인 권장

## 최근 완료 (2026-07-25, 프로필 편집에서 닉네임·실명 변경 차단)

MY탭 프로필 편집 시트에서 닉네임/실명을 더 이상 바꿀 수 없게 해달라는 요청.

- `editNickname`/`editRealName` 입력창을 `readonly`로 변경(회색 배경, "변경할 수 없어요" 안내 문구). 값은 그대로 표시되지만 수정 불가
- `saveProfile()`에서 두 필드 관련 검증·payload 전송 로직 완전히 제거 — 생년월일/휴대폰/지역/재학상태만 저장 대상. devtools로 readonly를 풀어도 애초에 서버로 안 보내니 반영 안 됨
- 브라우저에서 `readOnly` 속성·배경색 적용 확인, 콘솔 에러 없음. DB/RLS 변경 없음(서버 쪽 강제 차단은 아니고 클라이언트 UI+저장 로직 차원의 제한)

## 최근 완료 (2026-07-25, 휴대폰번호 입력창 스타일 통일)

프로필 편집의 휴대폰번호 필드만 다른 필드와 다르게 보이는 버그 발견. 공통 input 스타일 셀렉터(`input[type="text"], [type="number"], [type="date"]...`)에 `input[type="tel"]`이 빠져있어서 브라우저 기본 스타일로 보였던 것 — 셀렉터에 추가해 통일. 브라우저에서 계산된 스타일(패딩/테두리/폰트/높이) 비교로 확인 완료. DB 변경 없음

## 최근 완료 (2026-07-25, 완주 축하화면 반복 노출 버그 수정 + 다시보기 버튼)

완주 축하화면(confetti)이 같은 루틴인데도 로그인할 때마다 계속 뜬다는 제보.

- **원인**: "이미 축하함" 여부를 `localStorage`에만 기록(`hankkut_celebrated_{userId}_{routineId}`)하고 있어서, 브라우저 저장소가 지워지거나(iOS Safari 저장공간 정리 등) 다른 기기/브라우저로 접속하면 서버는 이미 완주 상태인데 로컬엔 기록이 없어 축하화면이 반복 노출됐음
- **수정**: `routine_participants.celebrated_at`(마이그레이션 `[2026-07-25h]`) 컬럼을 추가해 서버에 기록하는 방식으로 전환. `_maybeCelebrateCompletion()`이 이제 `me.celebratedAt` 유무로 판단, 띄우는 즉시 `celebrated_at`을 서버에 기록(RLS는 기존 `rp_update`의 본인 행 조건 그대로 재사용, 변경 불필요). localStorage 방식은 완전히 제거
- **다시보기 버튼 추가**: MY탭 "완주한 루틴" 목록 각 항목에 "🎉 다시보기" 버튼 추가 — 자동 노출 여부와 무관하게 언제든 같은 축하화면을 다시 열어볼 수 있음(`openCompleteSuccess(routineId)` 직접 호출)
- 브라우저 콘솔에서 목데이터로 검증: `celebratedAt` 있으면 자동 노출 안 됨 / 없으면 노출되며 즉시 로컬·서버에 기록됨 / "다시보기" 버튼 렌더링 확인
- **마이그레이션 `[2026-07-25h]` 실행 완료** — Supabase SQL Editor에서 사용자가 직접 실행, anon 키로 `routine_id,celebrated_at` 조회해 컬럼 존재 읽기전용 검증 완료

## 최근 완료 (2026-07-25, 프로필 편집 레이아웃 정리 + 소속 필드 추가)

- **생년월일/휴대폰번호를 한 줄에 배치** — 기존 세로 2줄을 `.date-row`(기존 모집기간/진행기간 등에서 쓰던 2열 grid 패턴) 재사용해 한 줄로 통일
- **"소속" 필드 추가** — 청소년(센터·학교 등)/끗짱(회사 등) 공용 자유텍스트, `profiles.affiliation`(마이그레이션 `[2026-07-25i]`). 값이 없으면 입력칸에 "없음"이 placeholder로 안내됨. 지역 아래·재학상태 위에 배치(두 역할 모두에 노출)
- `loadProfileAndEnter()`/`saveProfile()`/`openEditProfile()`에 반영. 브라우저에서 같은 줄 배치·placeholder 렌더링 확인, 콘솔 에러 없음
- **마이그레이션 `[2026-07-25i]` 실행 완료** — Supabase SQL Editor에서 사용자가 직접 실행, anon 키로 `id,affiliation` 조회해 컬럼 존재 읽기전용 검증 완료

## 최근 완료 (2026-07-25, QA 스프레드시트 신규 배치 대응 시작 — 1·2번)

새 QA 스프레드시트(모바일/청소년 위주) 접수. 사용자와 우선순위 확인 후(버그 먼저, 10·12번은 보류) 순서대로 진행.

1. **완주축하화면 등에서 양옆 흰줄 보임** — 원인은 예상보다 넓은 CSS 캐스케이드 순서 버그였음. `@media (max-width:480px) { .app, .nav, .overlay, .sheet, .success-scr, .complete-scr { max-width:100% } }`가 스타일시트 앞쪽(23번째 줄 부근)에 있었는데, `.nav`/`.overlay`/`.sheet`/`.success-scr`/`.complete-scr`의 기본 `max-width:390px` 규칙은 전부 그 뒤에 선언돼 있어서 — 동일 우선순위는 나중 선언이 이긴다는 CSS 규칙 때문에 이 5개 클래스에선 오버라이드가 계속 무시되고 있었음(`.app`만 우연히 미디어쿼리보다 먼저 선언돼 있어서 정상 동작). 미디어쿼리 블록을 스타일시트 맨 끝으로 옮겨서 해결 — 완주축하화면뿐 아니라 오버레이·바텀시트·하단탭바 전체에 영향. 브라우저에서 375px 뷰포트로 5개 클래스 전부 `max-width:100%` 적용 확인. DB 변경 없음
2. **편지쓰기 하면 "인증완료" 문구가 뜸** — 표면 버그 조사 중 더 큰 문제 발견: `submitLetter()`가 편지를 **DB에 전혀 저장하지 않고** 클라이언트 로컬(`state.letters`)에만 저장하고 있었음(`certSuccess`를 그대로 열어써서 문구도 잘못됨). `letters` 테이블·RLS(발신자 본인 또는 그 루틴 담당 끗짱/관리자만 조회 가능)는 이미 스키마에 준비돼 있었지만 클라이언트가 한 번도 연결한 적이 없었음 — 즉 이 기능은 지금까지 사실상 작동한 적이 없었을 가능성이 높음(관리자 계정으로 역할전환하며 테스트할 때만 로컬스토리지 공유로 되는 것처럼 보였을 뿐). 사용자 확인 후 "제대로 고치기"로 결정해 실제 연결:
   - `loadLetters()` 신규 — `letters` 테이블 실조회(RLS가 자동으로 본인 스코핑), 발신자 닉네임·루틴명 조인. `loadProfileAndEnter()`/`refetchOnResume()`에 추가
   - `submitLetter()` — `letters` insert로 변경(비동기), 성공 시 전용 "편지 전송 완료" 화면(`letterSuccess`, 💌 아이콘) 노출 — 기존처럼 `certSuccess`를 잘못 열던 버그 제거
   - `openLetter()` — 읽음 처리(`opened:true`)를 서버에도 반영(`letters` UPDATE)
   - DB/RLS 변경 없음(스키마에 이미 다 있었음). 브라우저에서 `sb.from` 스텁으로 편지 전송→성공화면 분기, 끗짱 MY탭 렌더링·읽음처리 흐름 검증 완료(실제 라이브 프로젝트에는 네트워크 호출 안 함)

`node --check`로 문법 검증 완료.

3. **완주 리포트 공유 - 바로 다운로드 안 됨 + 이미지 모서리 흰색** — 두 가지 원인이 겹쳐있었음
   - **흰색 모서리**: `#reportCardPreview`가 `border-radius:20px`인데 `html2canvas`를 옵션 없이 호출해서 기본값(흰 배경)으로 캔버스가 채워짐 — 둥근 모서리 바깥 영역이 흰 사각형으로 찍혀나옴. `html2canvas(el, {scale:3, backgroundColor:null})`로 수정
   - **iOS에서 바로 다운로드 안 됨**: iOS Safari는 `<a download>`를 무시하고 이미지를 그냥 새 탭에 열어버림(길게 눌러 수동 저장해야 함) — 지금까지는 이 사실을 모르고 다운로드된 것처럼 알림만 띄웠음. `_isIOSSafari()` 판별 추가, 이 경우만 새 탭으로 열고 "이미지를 길게 눌러 저장해주세요" 안내로 교체(그 외 브라우저는 기존 `<a download>` 클릭 그대로 유지)
   - `shareReportCard()`(공유하기 버튼)의 폴백 경로도 동일 함수(`_downloadBlob`)를 타므로 자동으로 같이 해결됨, 중복 알림만 안 뜨게 조정
   - DB 변경 없음. 브라우저에서 `html2canvas`/`navigator`를 스텁해 옵션 전달·iOS 분기·알림 문구 전부 검증 완료(iOS UA 4종 판별 정확도 포함)
4. **루틴 소개 밑에 "인증 방법" 추가** — 기존엔 "루틴 소개" 한 칸에 활동 설명과 인증 방법이 섞여 있었음. 사용자 확인 후 "새 필드 추가"로 결정. `routines.cert_guide`(선택, 마이그레이션 `[2026-07-25j]`) 신설 — index.html 끗짱 폼 + admin.html 양쪽에 "인증 방법(선택)" 입력칸 추가(루틴 소개 바로 아래), 청소년/끗짱 루틴 상세화면 둘 다 "🎯 이렇게 인증해 주세요" 섹션으로 노출(값 없으면 섹션 자체를 숨김). 브라우저에서 값 있을 때/없을 때 노출 조건 확인 완료
   - **마이그레이션 `[2026-07-25j]` 실행 완료** — Supabase SQL Editor에서 사용자가 직접 실행, anon 키로 `id,cert_guide` 조회해 컬럼 존재 읽기전용 검증 완료
5. **포인트/완주 기준 안내 추가** — 루틴마다 다르게 설정 가능한 완주 인정 비율(`completionRatioPct`)·인증당 지급 포인트(`certPointAmount`)가 지금까지 루틴 생성/수정 폼과 계산 로직에만 쓰이고, 정작 청소년이 보는 루틴 상세화면엔 전혀 안 보였음(MY탭에만 일반 설명이 있었음). 청소년/끗짱 루틴 상세화면(인증 방법 섹션 바로 아래)에 "✨ 인증 1회당 +N나다움 · N% 이상 인증하면 완주 인정" 노란 박스 추가. 자유루틴은 나다움 지급이 없으므로 노출 안 함(`!r.isFree` 조건). DB 변경 없음(기존 필드 재사용). 브라우저에서 고정기간/자유루틴 노출 조건 확인 완료
6. **루틴별 카테고리 구분(기획루틴/자유루틴) + 포인트 지급 여부 표시** — 사용자 확인 후 두 가지 결정: ①기존 "고정기간"이라는 내부 라벨을 사용자 노출용 "기획루틴"으로 통일(index.html 끗짱 폼 + admin.html 루틴 타입 선택지) ②이 배지(타입+포인트 여부)는 인증탭의 "내 참여 루틴" 카드에만 노출(`renderCertTab`) — 제목 아래에 "기획루틴"/"자유루틴" 색상 배지 + "인증 시 +N나다움"/"포인트 없음" 텍스트 추가. DB 변경 없음(기존 `isFree`/`certPointAmount` 필드 재사용). 브라우저에서 두 타입 카드 렌더링 확인 완료
7. **휴대폰 가로모드 고정(세로 전용)** — Screen Orientation API의 `lock()`은 fullscreen 모드에서만 동작하고 iOS Safari는 아예 미지원이라 실효성이 없어서, 순수 CSS로 처리. `@media (orientation:landscape) and (hover:none) and (pointer:coarse)`(터치 기반 기기에서만 감지) 조건에서 전체화면 안내 오버레이(`.rotate-lock-overlay`, "📱 세로 모드로 사용해주세요")를 덮어씌움 — 실제 레이아웃을 회전시키진 않지만 사실상 가로 사용을 막음. `hover:none`/`pointer:coarse` 조건 덕분에 데스크톱에서 넓은 창(폰 프레임 데모 모드 포함)을 쓸 때는 절대 안 뜸. 브라우저에서 `orientation:landscape` 감지·오버레이 내용 렌더링 확인(다만 이 headless 브라우저는 마우스 기반이라 실제 터치기기의 hover/pointer 판정까지는 재현 불가 — 실기기 확인 권장). DB 변경 없음

## 최근 완료 (2026-07-25, QA 스프레드시트 신규 배치 8~9번 — 인증탭 피드 + 접근권한)

7,8,9번은 전부 "인증탭에서 뭘 보여줄지"에 관한 내용이라 겹쳐서, 사용자와 세부 사양 확인 후 진행.

7. **인증탭에 인증글 피드 접근 추가** — 지금까지 인증 탭(`page-cert`)엔 "오늘의 인증"(할 일 목록)만 있고, 실제 인증글(사진+소감) 피드를 보는 경로가 아예 없었음(루틴탭/MY탭에서 루틴 카드를 눌러야만 `openRoutineFeed()` 진입 가능). 사용자 확인 후 "내가 참여한 루틴만, 루틴별로 모아서" 방식으로 결정 — `renderCertTab()`의 각 루틴 카드(오늘의 인증 목록) 전체를 클릭하면 그 루틴의 인증글 피드로 이동하도록 변경("인증하기" 버튼은 `stopPropagation`으로 분리), 그 아래 "완주한 루틴" 섹션(`_myCompletedRoutines()`)을 신설해 완주한 루틴의 인증글도 모아볼 수 있게 함(활동 중인 루틴이 없어도 완주 목록은 보이도록 빈 상태 조건 조정)
8. **인증하기 폼에 "인증 방법" 안내 추가** — 4번(루틴 상세화면)과 별개로, 실제 사진 올리는 인증 폼(`certFormOverlay`) 안에도 그 루틴의 인증 방법(`cert_guide`)을 상단에 보여주도록 추가(`openCertForm`). 값 없으면 배너 자체를 숨김
9. **인증글 접근권한을 참여자만으로 제한 (보안 수정)** — 조사 중 실제 보안 구멍 발견: `certifications` SELECT RLS가 로그인 여부만 체크(`auth.uid() IS NOT NULL`)하고 있어서, 로그인한 사용자면 **참여하지 않은 루틴의 인증 사진·소감도 전부 조회 가능**했음(2026-07-25b에서 비로그인 차단은 했지만 로그인 사용자 간 스코핑은 안 돼있었음). 사용자 확인 후 "참여자만" 원칙으로 단순화해 처리:
   - **DB**: `cert_select` 정책을 참여자 본인(`user_id`) 또는 그 루틴의 참여 승인된 사람(`routine_participants` approved) 또는 담당 끗짱/작성자(`led_by`/`created_by`) 또는 관리자만 조회 가능하도록 축소(마이그레이션 `[2026-07-25k]`)
   - **클라이언트**: `openRoutineFeed()`에 참여자 여부 가드 추가 — 참여자/담당 끗짱/관리자가 아니면 "🔒 이 루틴에 참여한 사람만 인증글을 볼 수 있어요" 안내로 대체(서버가 이미 막지만, "인증글이 없어요"처럼 오해하지 않도록 이유를 명확히 안내)
   - 브라우저에서 참여자/비참여자 케이스 둘 다 검증 완료(목데이터). 실제 라이브 프로젝트에는 네트워크 호출 안 함
   - **마이그레이션 `[2026-07-25k]` 실행 완료** — Supabase SQL Editor에서 사용자가 직접 실행, anon 키로 비로그인 차단 유지 확인(참여자별 세부 스코핑은 실기기 계정 2개로 추가 확인 권장)

`node --check`로 문법 검증 완료.

## 최근 완료 (2026-07-25, QA 10번 — 완료 루틴을 홈 기본 목록에서 숨김)

"완료 루틴은 안 보이게 해야 하나?" 논의 끝에 결정: 완주 루틴을 보는 경로가 이미 MY탭·인증탭(둘 다 오늘 추가/확인됨) 두 곳이나 있어서, 루틴탭(홈)의 "전체" 기본 목록에까지 있으면 "지금 뭘 시작할 수 있나" 탐색 목적과 안 맞고 목록만 길어진다는 데 합의 — 완전히 없애지 않고 기본 필터에서만 제외, 기존 "완료" 칩으로는 여전히 볼 수 있게 함.

- `renderRoutineGrid()`의 `type==='all'` 케이스에서 `status==='done'` 루틴 제외. "완료" 칩(`type==='done'`)은 기존 로직 그대로라 영향 없음
- 끗짱 쪽(`kFilterRoutine`)은 "전체"가 원래 다른 담당자 루틴까지 보는 별도 목적(둘러보기)이라 이번엔 안 건드림
- 브라우저에서 "전체"엔 완료 루틴이 안 뜨고 "완료" 칩엔 정상 노출되는 것 확인, 콘솔 에러 없음. DB 변경 없음

## 최근 완료 (2026-07-25, 인증글 좋아요·댓글도 참여자만 볼 수 있도록 제한)

`[2026-07-25k]`로 `certifications`를 참여자만 볼 수 있게 막은 직후, 같은 구멍이 `cert_likes`/`cert_comments`에도 남아있는 걸 발견 — 둘 다 `auth.uid() IS NOT NULL`만 체크해서 비참여자가 REST로 직접 조회하면 댓글 내용이 새어나갈 수 있었음.

- `cl_select`/`cc_select` 정책을 "그 인증글(`certifications`)을 볼 수 있는 사람만" 기준으로 축소(`cert_select`와 동일 조건 — 작성자 본인/참여자/담당 끗짱/관리자). 마이그레이션 `[2026-07-25l]`
- **클라이언트 코드 변경 없음** — `loadCertFeed()`가 이미 좋아요/댓글을 가져온 뒤 JS에서 `cert_id`로 매칭하는 구조라, 서버가 반환하는 행 수만 자연히 줄어들 뿐 로직은 그대로 맞물림. 좋아요/댓글의 INSERT·DELETE는 원래부터 본인 것만 가능해서 이번 변경과 무관
- **마이그레이션 `[2026-07-25l]` 실행 완료** — Supabase SQL Editor에서 사용자가 직접 실행, anon 키로 비로그인 차단 유지 확인

## 최근 완료 (2026-07-25, 완주 리포트 "이미지로 저장" 버튼 제거)

실기기 검증 중 발견 — "이미지로 저장" 버튼을 누르면(iOS Safari의 `<a download>` 미지원 폴백으로 새 탭에 이미지를 여는 방식) 검은 화면만 나오는 문제 확인. 사용자 제안대로 처리: 어차피 iOS 공유 시트(`navigator.share`)에 "이미지 저장" 옵션이 이미 있어서 버튼 두 개가 중복이었음 — "📥 이미지로 저장" 버튼과 `downloadReportCard()` 함수를 완전히 제거하고 "📤 공유하기" 하나만 남김. `shareReportCard()`는 그대로 유지되며, 공유 API를 못 쓰는 브라우저에선 내부적으로 기존 다운로드 폴백(`_downloadBlob`)으로 자동 전환됨. 브라우저에서 버튼 1개만 남은 것 확인, 콘솔 에러 없음. DB 변경 없음
- **남은 것**: 공유 API 자체가 안 되는 구형 브라우저에서는 여전히 `_downloadBlob` 폴백을 타므로, 그 경로의 검은 화면 재현 가능성은 남아있음(대부분의 최신 iOS/Android는 공유 API가 되므로 실사용 영향은 낮을 것으로 예상) — 재현되면 추가 조치 필요

## 최근 완료 (2026-07-25, 완주 후 편지쓰기 화면 뒤로가기 버그 수정)

실기기 검증 중 발견 — 루틴 완주 화면에서 "후기"/"편지" 버튼을 눌러 편지 작성 화면으로 들어간 뒤 뒤로가기를 누르면, 방금 있던 화면(후기/편지/공유 버튼 행)이 아니라 그 전전 화면으로 건너뛰는 문제.

- **원인**: `openRoutineFeed()`/`openRoutineDetail()`의 "후기"/"편지" 버튼이 `closeOverlay('routineFeedOverlay'/'routineDetailOverlay')`를 먼저 호출한 뒤 다음 화면을 열고 있었음 — 뒤로가기 히스토리 스택(`_navStack`)은 "X버튼으로 먼저 닫힌 항목은 그대로 스택에서 제거"하는 방식이라, 다음 화면(편지 등)을 닫아도 이미 사라진 그 이전 화면으로는 못 돌아가고 그 전전 화면이 나타났음
- **수정**: 두 버튼에서 `closeOverlay(...)` 호출 제거 — 편지/후기 화면이 원래 화면 위에 그대로 쌓이도록(DOM 순서상 `letterOverlay`가 더 뒤에 있어 자연스럽게 위에 렌더링됨, `reviewWriteSheetBg`는 `.sheet-bg`라 z-index가 더 높아 항상 위). 이제 뒤로가기 한 번이면 방금 있던 화면으로 정확히 돌아감
- 완주 축하 화면(`completeSuccess`)의 "완주 리포트 공유"/"AI 리포트" 버튼은 애초에 히스토리 스택에 안 걸리는 별개 화면이라 이번 버그와 무관, 손 안 댐. 커뮤니티 글쓰기 선택 시트(`writeSheetBg`)의 후기/편지 버튼도 같은 패턴이지만 letterOverlay(z-index 200)가 그 시트(z-index 300)보다 아래라 단순 제거 시 화면이 안 보이게 될 위험이 있어 이번엔 그대로 둠(필요시 별도 처리)
- 브라우저에서 팝스테이트 시뮬레이션으로 뒤로가기 1회 후 정확히 이전 화면(routineFeedOverlay)으로 돌아가는 것 확인, 콘솔 에러 없음. DB 변경 없음

## 최근 완료 (2026-07-25, 루틴별 "실시간 촬영만 허용" 옵션 추가)

인증 사진을 갤러리에서 못 고르고 그 자리에서 촬영한 사진만 올릴 수 있는 루틴 옵션 요청 — 기존 "지금 찍기"(카메라 촬영+촬영시각 워터마크, 2026-07-14 완료) 기능을 그대로 재사용해서 구현.

- `routines.camera_only`(기본 false) 추가(마이그레이션 `[2026-07-25m]`). index.html 끗짱 폼 + admin.html 양쪽에 "📸 실시간 촬영만 허용" 체크박스 추가(고정 체크박스와 동일 위치·패턴)
- 인증 폼(`renderCertPhotoGrid`)에서 이 옵션이 켜진 루틴이면 갤러리 업로드 타일("+")을 아예 안 보여주고 "📸 지금 찍기" 타일만 남김, 상단에 "이 루틴은 실시간 촬영만 가능해요" 안내 배너 노출
- 방어 코드: `handleCertPhotos()`에도 가드 추가 — 혹시 갤러리 입력이 다른 경로로 트리거돼도 실시간 촬영 전용 루틴이면 알림과 함께 거부
- 별도 RLS 변경 불필요(기존 routines_update 재사용). 브라우저에서 일반/실시간전용 루틴 각각 타일 노출 조건, 알림 문구, 콘솔 에러 없음 확인
- **마이그레이션 `[2026-07-25m]` 실행 완료** — Supabase SQL Editor에서 사용자가 직접 실행, anon 키로 `id,camera_only` 조회해 컬럼 존재 읽기전용 검증 완료

## 최근 완료 (2026-07-25, 완주 인정 비율 커스터마이징 기능 철회 — %를 일수로 안내)

"완주 인정 비율 67%"라는 숫자가 참가자에게 직관적이지 않다는 피드백에서 시작. 논의 끝에 %를 일수로 바꿔 보여주는 것에서 더 나아가, 애초에 루틴마다 비율을 다르게 설정하는 기능 자체를 없애고 전체 67%로 고정하기로 결정(2026-07-25 오전에 추가했던 "루틴별 완주 인정 비율 설정" 기능을 철회).

- **폼에서 입력칸 완전 제거** — index.html 끗짱 폼(`rRatioFieldWrap`/`rRatio`) + admin.html(`rtRatioFieldWrap`/`rtRatio`) 양쪽에서 필드·검증·payload 참조 전부 제거. 생성/수정 시 `completion_ratio_pct`는 이제 항상 67로 고정 전송
- **기존 데이터도 67%로 되돌림** — 혹시 그새 다르게 설정된 루틴이 있었을 경우를 대비해 `UPDATE routines SET completion_ratio_pct = 67 WHERE completion_ratio_pct IS DISTINCT FROM 67;` 포함(마이그레이션 `[2026-07-25n]`)
- **화면엔 %를 아예 안 보여주고 구체적인 일수로 안내** — `_completionGoal(r)` 공용 헬퍼 신설(루틴 기간 기준 총 일수·완주 목표 일수 계산). 청소년/끗짱 루틴 상세화면의 안내 박스를 "N% 이상 인증하면 완주"에서 "총 N일 중 M일 이상 인증하면 완주 인정"으로 교체, 끗짱 인증글 피드(`openKRoutineFeed`)의 "완주 기준 (목표의 N% 이상)"도 동일하게 일수 기준으로 교체. MY탭 "완주 기준" 설명 문구도 특정 %를 언급하지 않도록 일반화
- 브라우저에서 입력칸 완전 제거 확인, 상세화면에 "총 21일 중 14일 이상" 형태로 정확히 계산되어 표시되는 것 확인, 콘솔 에러 없음
- **마이그레이션 `[2026-07-25n]` 실행 완료** — Supabase SQL Editor에서 사용자가 직접 실행

## 최근 완료 (2026-07-25, QA 7~9번 대응 - 끗짱 루틴 인증글 화면 재설계)

새 QA 배치(끗짱/일반끗짱 위주)에서 "인증글이 안 보인다"는 신고 3건(3·4·8번)을 먼저 조사. 처음엔 그날 앞서 처리한 "인증글은 참여자만 볼 수 있도록" 보안 수정(RLS)의 회귀를 의심했으나, 조사 결과 **근본 원인은 하나로 수렴**함을 확인:

- **원인**: 끗짱용 루틴 화면(`openKRoutineFeed`)이 "오늘 인증했는지 안 했는지"만 보여주는 하루짜리 현황판으로만 설계돼 있었고, 실제 인증 사진·소감을 보여주는 "전체 기간 피드"가 애초에 없었음(청소년 쪽 `openRoutineFeed`엔 있음). 게다가 이 화면의 참여자 목록은 `routine_participants`(정식 신청·승인) 기준이라, 담당 끗짱이 신청 절차 없이 자기 루틴에 직접 인증해도(기존부터 허용된 동작) 그 목록에 안 잡혀서 본인 인증글은 아예 클릭할 방법이 없었음
  - 3번(참여자 사진 미리보기 안 보임), 4번(끗짱 본인 인증글 안 보임), 8번(다음날 들어가면 전날 기록 사라짐)이 전부 이 하나의 설계 공백에서 나온 증상이었음
- **수정**: 청소년 쪽 인증글 그리드 마크업을 `_certFeedGridHtml(certs)` 공용 함수로 뽑아내고(기존 `openRoutineFeed`도 이걸 쓰도록 리팩터, 동작 변화 없음), `openKRoutineFeed`에 "오늘 인증 현황"(기존 그리드, 그대로 유지) 아래에 **"📸 전체 인증 기록(본인 인증 포함)"** 섹션을 신설 — `state.certFeed`를 그대로 가져와 사진 썸네일과 함께 전체 기간 기록을 보여주고, 담당 끗짱 본인이 올린 인증글도 자연히 포함됨(RLS는 이미 본인 것·담당 루틴 것을 허용하고 있어서 서버쪽엔 손 안 댐)
- 브라우저에서 목데이터로 확인: 정식 참여자 아닌 끗짱 본인 인증글이 새 섹션에 노출·클릭 가능, 청소년 쪽 기존 피드도 리팩터 후 정상 동작, 양쪽 빈 상태 문구 정상 표시. 콘솔 에러 없음. DB 변경 없음

## 최근 완료 (2026-07-25, QA 신규배치 2번 — 루틴 소개칸 줄바꿈 안 됨)

루틴 소개(`r.desc`)·인증 방법(`r.certGuide`)을 보여주는 곳들에 `white-space:pre-wrap`이 누락돼 있어서 줄바꿈이 무시되던 버그. 예전에 게시글/인증글/댓글에서 같은 이유로 고쳤던 것과 동일한 패턴(2026-07-24 QA 대응 2번). 청소년/끗짱 루틴 상세화면의 "루틴 소개"·"이렇게 인증해 주세요" 표시 4곳 + 인증 폼 안의 인증 방법 배너(`certGuideText`)까지 전부 `white-space:pre-wrap` 추가. 브라우저에서 개행 포함 텍스트로 렌더링·computed style 확인 완료, 콘솔 에러 없음. DB 변경 없음

## 최근 완료 (2026-07-25, QA 신규배치 1·11번 — 끗짱 "진행 중" 필터 추가)

두 항목이 사실 같은 문제였음: 1번("루틴-진행 중 카테고리 추가해도 좋을 것 같아")과 11번("모집중, 완료에는 루틴이 안보임")을 같이 조사해보니, 끗짱 루틴탭 필터 칩이 "전체/모집 중/완료" 3개뿐이라 **진행 중(active) 상태인 루틴을 걸러볼 방법 자체가 없었음** — "전체"(org-wide 전체 루틴)에는 보이지만 "모집 중"/"완료" 어디에도 안 걸려서 마치 루틴이 사라진 것처럼 보였던 것. 코드 주석에도 원래 "진행중/모집중/완료" 3종 스코핑을 의도했던 흔적이 있었지만 칩 자체가 누락돼 있었음.

- `kRoutineFilterBar`에 "진행 중" 칩 추가(모집 중과 완료 사이), `kFilterRoutine('active', ...)`가 `status==='active' && !_isFreeOpen(r)` 기준으로 필터링(상시 열려있는 자유루틴은 "모집 중" 쪽에 남도록 제외해 중복 노출 방지)
- 브라우저에서 진행중/모집중/완료 루틴 각각 만들어 4개 칩 전부 올바른 항목만 보여주는 것 확인, 콘솔 에러 없음. DB 변경 없음

## 최근 완료 (2026-07-25, QA 신규배치 7번 — 댓글이 2개씩 달리는 버그)

"댓글에 댓글이 2개씩 달림(매번 그러지는 않음)"(PC/끗짱). 한글 IME(조합) 입력 중 Enter를 누르면 일부 Windows 브라우저에서 `keydown` 이벤트가 두 번 발생하는 잘 알려진 현상이 원인으로 보임 — 조합 완료용 Enter와 실제 Enter가 각각 `submitCommentFromBar()`를 호출해 댓글이 중복 등록됨(한글 입력이라 PC/끗짱에서 자주 보이고 "매번 그러지는 않는" 것과도 일치).

- **1차 방어**: 댓글 입력창(`rdCommentInput`)의 `onkeydown` 핸들러에 `!event.isComposing` 조건 추가 — IME 조합 중 발생하는 Enter는 무시
- **2차 방어**: `submitCommentFromBar()`에 `_submittingComment` 진행 중 플래그 추가(try/finally로 항상 해제) — IME 문제가 아니어도 전송 버튼 연타 등 다른 경로의 중복 제출까지 함께 방어. 인증 댓글·게시글 댓글 둘 다 이 함수를 공유해서 한 번의 수정으로 둘 다 커버됨
- 브라우저에서 `submitCommentFromBar()`를 연속 호출해도 실제 insert는 1번만 나가는 것 확인(스텁으로 삽입 호출 횟수 검증). 콘솔 에러 없음. DB 변경 없음

## 최근 완료 (2026-07-25, QA 신규배치 6번 — 편지 받아도 알림 안 뜸)

오늘 세션에서 편지 기능을 실제 DB(`letters` 테이블)에 처음 연결했는데, 수신 알림(푸시)이 빠져있었던 것을 발견. 기존 `on_post_comment_push()`와 동일한 패턴으로 `on_letter_push()` 트리거 신설 — 편지가 insert되면 그 루틴의 담당 끗짱(`led_by`, 없으면 `created_by`)에게 `notify_push()`. 클라이언트 코드 변경 없음(서버 트리거만으로 완결). 마이그레이션 `[2026-07-25o]`
- **마이그레이션 `[2026-07-25o]` 실행 완료** — Supabase SQL Editor에서 사용자가 직접 실행

## 최근 완료 (2026-07-25, QA 신규배치 9·10번 — 끗짱 루틴 수정·참여자 승인 권한을 관리자만으로 축소)

지금까지의 핵심 설계("담당 끗짱이 직접 승인/거절, 자기 루틴 수정 가능")를 뒤집는 결정이라 사용자와 범위 확정 후 진행:
- 담당 끗짱(개인 계정)은 앞으로 **승인된 참여자 모니터링만** 가능(콕 찌르기, 본인 직접 인증 등은 그대로 유지)
- 루틴 수정·폐강 처리·신청자 승인/거절/승인취소는 **관리자(유스보이스)만** 가능
- 관리자가 `loginType='both'`로 끗짱 화면을 미리볼 때는 기존처럼 전체 권한 그대로 유지(관리자 계정 자체가 기준이라 `is_admin()`만으로 자연스럽게 구분됨)

- **작업 중 발견한 부수 버그**: `openKRoutineDetail()`의 완주 기준 안내 박스가 오늘 앞서 처리한 "%→일수 안내" 리팩터에서 빠져있어서 "N% 이상 인증하면 완주"라는 옛 문구가 그대로 남아있었음 — 이번에 같이 `_completionGoal()` 기준 일수 표기로 수정
- **DB**: `routines_update`/`rp_update` RLS 정책에서 `led_by` 조건 제거(마이그레이션 `[2026-07-25p]`) — `created_by`(항상 관리자, 루틴 개설이 관리자 전용이라)와 `is_admin()`만 남김
- **클라이언트**: `openKRoutineDetail()`에 `isAdmin = state.loginType==='both'` 가드 추가 — "루틴 수정"·"폐강 처리" 버튼은 관리자에게만 노출, 신청자 목록도 관리자는 기존처럼 전체(대기/승인/거절)+승인·거절·취소 버튼을 보고, 일반 끗짱은 "참여자 목록"(승인된 사람만) 읽기 전용으로 축소 표시. "신청 승인·거절은 유스보이스에서 진행해요" 안내 문구 추가
- 브라우저에서 일반 끗짱/관리자 두 계정 유형으로 각각 렌더링 확인(일반 끗짱: 수정·폐강·승인 버튼 전부 안 보이고 승인자만 노출 / 관리자: 기존 그대로 전부 노출). 콘솔 에러 없음
- **마이그레이션 `[2026-07-25p]` 실행 완료** — Supabase SQL Editor에서 사용자가 직접 실행

## 최근 완료 (2026-07-25, QA 3차 신규배치 8·11번 — 관리자 루틴 작성 사진 업로드 + 상태 판정 오류)

8. **관리자 루틴 작성 시 인증 예시 사진 업로드 실패** — admin.html의 `handleRoutinePhotoUpload()`가 원본 파일명을 그대로 스토리지 경로에 써서(`admin_${Date.now()}_${file.name}`), 한글·공백·특수문자가 든 파일명(폰 사진 등)에서 업로드가 실패했음. index.html 쪽처럼 확장자만 뽑아 안전한 경로(`admin_${Date.now()}.${ext}`)로 구성하도록 수정
11. **모집/진행 기간이 루틴 상태에 반영 안 됨** — 사용자 확인 후 "진행 시작일이 돼야 진짜 진행중으로 전환"으로 결정. 두 가지 원인이 겹쳐있었음:
    - **근본 원인**: `approveApplicant()`가 첫 승인 "즉시" 루틴 status를 `active`로 바꾸고 있어서, 실제 진행 시작일 전에 승인해도 "진행 중"으로 잘못 표시됐음 → 이 즉시 전환 로직 제거. 대신 `loadRoutines()`의 날짜 기반 자동전환(기존엔 종료일 지난 진행중→완료만 있었음)에 "시작일이 된 모집중→진행중" 케이스를 추가해 일원화
    - **부수 발견**: 오늘 앞서 처리한 끗짱 권한 축소(9·10번, `routines_update`에서 `led_by` 제거)로 인해, 이 날짜 기반 자동전환의 write-back이 일반 끗짱/청소년 세션에서는 더 이상 반영되지 않게 됐음(RLS가 조용히 막음) — write-back 조건에서도 `led_by` 체크 제거, `admin.html`이 로드될 때도 동일 로직으로 보정하도록 추가(관리자 세션은 항상 쓰기 권한이 있어서 오히려 여기가 더 확실한 보정 시점)
    - admin.html은 지금까지 DB의 raw `status`만 그대로 보여주고 날짜 기준 재계산이 전혀 없었음 — `loadData()`에 동일한 effective-status 계산 추가(모집 마감일이 지났는데도 "모집 중"으로 보이던 관리자 쪽 표시 문제 해결)
- 브라우저에서 4가지 케이스(모집중 유지/시작일 지나 진행중 전환/종료일 지나 완료 전환/진행중 유지) 전부 정확히 계산되는 것 확인, admin.html 콘솔 에러 없음. DB 변경 없음(로직 수정만, 스키마 변경 없음)

## 최근 완료 (2026-07-25, QA 3차배치 5·6·7·9·10번 — 배너/리워드/루틴 필터)

5. **배너 등록 시 이미지 첨부 안 됨** — 8번(루틴 사진)과 똑같은 원인. `handleBannerImageUpload()`가 원본 파일명을 그대로 스토리지 경로에 써서 한글·공백 파일명에서 업로드 실패 — 확장자만 뽑는 안전한 경로로 수정. 다른 업로드 코드(청소년 서류·인증사진·후기사진·루틴표지)도 전부 점검했는데 이미 안전한 패턴이라 이 두 곳만 문제였음
6. **배너 내용 글자수 제한 없음** — `bnBody`에 `maxlength` 자체가 없어서 관리자가 아무리 길게 써도 막히지 않았고, 배너 표시 영역(75px 높이 스트립)이 좁아서 긴 내용은 화면에서 잘려 보였음. `maxlength="30"` + 글자수 카운터 추가(수정 화면 프리필·초기화 시에도 카운터 동기화)
7. **리워드 이미지 URL 필드 → 업로드로 교체** — 사용자 확인 후 배너/루틴과 동일한 "업로드 버튼" 패턴으로 통일. `reward-images` 스토리지 버킷 신설(마이그레이션 `[2026-07-25q]`), URL 직접 입력 필드는 숨김 처리하고 업로드 시 자동으로 채워지도록 변경
9. **루틴 "전체 상태" 필터에 보관된 루틴이 안 보임** — `_filteredRoutines()`가 "전체 상태" 선택 시에도 보관된(`archived`) 루틴을 제외하고 있었음 — 그 조건 제거, 이제 "전체"는 말 그대로 전부 보여주고 "보관됨" 필터로 따로 걸러볼 수 있음(기존 동작 그대로 유지)
10. **루틴 상태 필터에 "폐강" 옵션 없음** — `rStatusFilter` 드롭다운에 "폐강"(`cancelled`) 옵션 추가. 필터링 로직 자체는 이미 범용이라 옵션만 추가하면 바로 동작함
- 브라우저에서 배너 30자 카운터, 리워드 업로드 필드(hidden으로 전환), 루틴 필터(전체=보관 포함, 폐강 필터 단독) 전부 확인. 콘솔 에러 없음
- **마이그레이션 `[2026-07-25q]` 실행 완료** — Supabase SQL Editor에서 사용자가 직접 실행, anon 키로 오브젝트 리스트 조회해 버킷 존재 확인

## 최근 완료 (2026-07-25, QA 3차배치 2·3·4번 — 청소년 알림/UX 개선)

2. **알림을 눌러도 해당 화면으로 이동 안 함** — 두 가지 원인이 겹쳐있었음
   - **실제 푸시 알림**: `Client.navigate()`가 iOS Safari 등 일부 환경에서 안정적으로 동작 안 해서, 앱이 이미 열려있으면 focus만 되고 URL(탭)은 그대로 "홈"에 남아있는 것처럼 보였음 — service-worker가 새로고침 없이 `postMessage`로 탭 정보를 페이지에 전달하고, 페이지가 직접 `switchTab`/`kSwitchTab`을 호출하도록 변경(더 이상 `navigate()`에 의존 안 함)
   - **인앱 알림 내역함**: 목록 항목에 클릭 핸들러 자체가 없어서 눌러도 아무 반응이 없었음 — 지금은 알림별로 어떤 인증글/편지인지까지는 저장 안 하고 있어서, 우선 실제 푸시와 동일하게 클릭 시 MY탭으로 이동하도록 추가(향후 알림에 딥링크 정보를 저장하게 되면 더 구체적으로 개선 가능)
3. **포인트 수동 지급/차감 시 알림 없음** — admin.html `grantNadaeum()`이 `nadaeum_log`엔 기록하면서 정작 알림 발송 코드가 없었음 — "알림 보내기" 탭과 동일한 `send-push` Edge Function 호출 추가(금액이 양수/음수인지에 따라 문구 자동 조정)
4. **총 인증 수/소통 수 클릭 시 내 기록 모아보기** — MY탭 통계 카드에 클릭 핸들러 추가. "총 인증 수" → 내가 올린 인증글만 모아 사진 그리드로(기존 `_certFeedGridHtml()` 재사용), "소통 수" → 내가 쓴 글(후기/공지) + 내가 단 댓글(인증글 댓글·게시글 댓글 통합, 시간순)을 한 화면에서 모아보기. 클릭하면 원글로 바로 이동
- 브라우저에서 인증글/글/댓글 각각 본인 것만 필터링되는지, 빈 상태 문구, 콘솔 에러 없음 확인. DB 변경 없음
- **부수 발견(별도 작업으로 분리)**: `notify_push()` DB 함수에 RPC 호출 권한 제한이 없어서, 이론상 아무 로그인 사용자나 `sb.rpc('notify_push',...)`를 직접 호출해 다른 사용자에게 가짜 알림을 보낼 수 있는 상태 — 오늘 작업 범위 밖이라 별도 작업으로 분리해둠(task_65893303)

## 최근 완료 (2026-07-25, notify_push() 직접 RPC 호출 차단 — 보안 수정)

위에서 분리해둔 부수 발견 건을 별도로 처리. `notify_push()`가 SECURITY DEFINER인데 EXECUTE 권한 제한이 없어서, Supabase가 public 스키마 함수를 PostgREST RPC로 자동 노출하는 특성상 로그인한 사용자 누구나 `sb.rpc('notify_push', {target_user:'<임의 uuid>', ...})`를 브라우저에서 직접 호출해 다른 사용자에게 가짜 알림(푸시+알림함 기록)을 보낼 수 있는 상태였음.

- index.html/admin.html 전체에서 `sb.rpc(` 호출 자체가 한 군데도 없는 것을 확인(전부 트리거 함수 내부의 `PERFORM notify_push(...)`로만 호출됨) — 트리거는 함수 소유자 권한으로 실행되므로 `authenticated`/`anon`/`PUBLIC`의 RPC 실행 권한만 제거해도 기존 알림 기능(승인/거절/댓글/좋아요/편지/포인트 지급 등 전체)엔 전혀 영향 없음
- `REVOKE EXECUTE ON FUNCTION notify_push(uuid,text,text) FROM PUBLIC, authenticated, anon;` (마이그레이션 `[2026-07-25r]`)
- **마이그레이션 `[2026-07-25r]` — 사용자가 Supabase SQL Editor에서 직접 실행 필요**

## 최근 완료 (2026-07-25, 커뮤니티 새 글 하단탭 배지)

QA 신규 6항목 배치 중 3번: "커뮤니티에서 새 글 올라오면 하단탭에 N 알림이 보면 좋겠음". DB 변경 없이 클라이언트 로컬 상태만으로 구현.

- 청소년 하단탭(`nav-comm`)·끗짱 하단탭(`knav-review`) 모두에 배지 span 추가 (`navCommBadge`/`navReviewBadge`), 기존 인증탭 배지(`navCertBadge`)와 동일한 스타일 재사용
- `loadPosts()`가 만드는 글 객체에 `ts: new Date(p.created_at).getTime()` 추가
- 사용자별 마지막 열람 시각을 로컬스토리지(`hankkut_comm_lastseen_{userId}`)에 저장 — 기존 `PUSH_NUDGE_KEY` 7일-스누즈 패턴과 동일한 방식
- `updateCommNavBadge()`: 본인 글 제외, `lastSeen` 이후 올라온 글 수를 세어 두 배지에 동시 반영(9개 초과는 "9+")
- `markCommSeen()`: 커뮤니티 탭 진입 시 `lastSeen`을 현재 시각으로 갱신하고 배지 즉시 제거 — `switchTab('comm')`과 `kSwitchTab('review')` 양쪽에 연결
- `loadProfileAndEnter()`/`refetchOnResume()`에서도 배지 갱신하도록 역할 무관하게 호출 (기존엔 청소년만 호출하던 것을 끗짱까지 확장)
- 검증: 브라우저에 mock `state.posts`/`currentUser` 주입 후 `updateCommNavBadge()`/`markCommSeen()`/`kSwitchTab('review')` 직접 호출 — 본인 글 제외 카운트, 열람 후 배지 사라짐, 콘솔 에러 없음 확인 (청소년/끗짱 양쪽)

## 최근 완료 (2026-07-25, 루틴 수정 실패 시 조용히 "성공" 뜨는 문제 방지)

QA 신규 6항목 배치 중 4번: "상단 고정 체크하고 저장해도 고정이 안 됨" 제보 조사.

- **원인**: 코드 버그 아님 — 제보한 끗짱 테스트 계정이 "루틴 수정 관리자 전용화"(2026-07-25, [b19b59c](https://github.com/youthitdev/youthit-routine/commit/b19b59c9)) 배포 이전의 **캐시된 구버전 페이지**를 보고 있었음. 강력 새로고침 후 재확인하니 비관리자 계정엔 "루틴 수정" 버튼 자체가 정상적으로 안 보임(확인 완료)
- 다만 조사 과정에서 구조적 문제를 하나 발견: `sb.from('routines').update(payload).eq('id', id)`는 RLS(`routines_update` 정책)에 막혀 **0건 반영돼도 에러를 던지지 않음** — 캐시 지연이든 다른 권한 엣지케이스든, 권한 없는 계정이 어떻게든 저장 버튼을 누르면 "저장 완료" 메시지만 뜨고 실제로는 아무 것도 안 바뀌는 조용한 실패가 가능했음
- index.html `submitEditRoutine()`, admin.html `submitRoutineAdmin()`(수정 분기)에 `.select('id')`를 붙여 반영된 행 수를 확인하고, 0건이면 "이 루틴을 수정할 권한이 없어요" 에러를 명시적으로 띄우도록 수정
- DB 변경 없음(클라이언트 코드만 수정)
- 검증: 브라우저에서 `sb.from('routines').update` 응답을 각각 `data:[]`(권한없음 시뮬레이션)/`data:[{id}]`(정상)로 스텁 — 0건일 때 정직한 에러 알림이 뜨고, 정상일 때는 기존처럼 성공 화면이 뜨는 것 확인 (index.html/admin.html 양쪽)

## 최근 완료 (2026-07-25, 끗짱 루틴탭 모집중/진행중/완료 필터가 텅 비어 보이던 문제)

QA 신규 6항목 배치 중 6번: "루틴탭 전체 탭에서는 모집중/진행중/완료 루틴이 다 보이는데, 각 상태별 필터 탭에서는 아무것도 안 보임".

- **원인**: `kFilterRoutine()`이 "전체" 탭은 조직 전체 루틴(`state.routines`)을 보여주면서, 모집중/진행중/완료 필터는 `_kMyRoutines()`(내가 담당(led_by)한 루틴만)로 스코핑하고 있었음 — 이전 세션에 "전체=둘러보기, 나머지=내 담당만"으로 의도한 설계였는데, 담당 중인 루틴이 없는 계정에서는 상태별 탭이 전부 텅 비어 보이는 혼란스러운 경험이 됨(빈 상태 안내 문구도 없어서 더 헷갈림)
- 담당 여부와 무관하게 상태 필터도 조직 전체로 통일 — `kFilterRoutine()`의 `base`를 항상 `state.routines`로 변경(내 담당 루틴만 보는 홈/인증탭/통계의 `_kMyRoutines()` 사용은 그대로 유지)
- DB 변경 없음(클라이언트 코드만 수정)
- 검증: 브라우저에 다른 사람이 담당(`ledBy:'other'`)인 모집중/진행중/완료 루틴을 mock으로 주입 — "전체"뿐 아니라 "모집중"/"완료" 필터에서도 정상적으로 보이는 것 확인, 콘솔 에러 없음

## 최근 완료 (2026-07-29, 루틴 상세 완주 인정 배너 줄바꿈 깨짐)

루틴 상세 화면의 "✨ 인증 1회당 +N나다움 · 총 X일 중 Y일 이상 인증하면 완주 인정" 배너에서 "당", "움", "정" 같은 글자가 단어 중간에서 잘려 다음 줄로 넘어가던 문제.

- 원인: 배너 div가 `display:flex`였는데, 텍스트 콘텐츠가 통째로 하나의 익명 flex 아이템으로 취급되면서 `word-break:keep-all` 없이 좁은 폭에 맞춰 임의의 글자 단위로 줄바꿈됨
- `display:flex` 제거(아이콘 정렬용이 아니라 단순 텍스트라 불필요)하고 `word-break:keep-all` 추가 — 청소년/끗짱 루틴 상세 화면 두 곳(동일 마크업) 모두 수정
- DB 변경 없음
- 검증: 브라우저에 동일 마크업 렌더링 — 단어 단위로 자연스럽게 줄바꿈되는 것 확인

## 최근 완료 (2026-07-29, 지원사업 심사 준비 중 발견한 커뮤니티·루틴 버그 3건)

지원사업 심사위원용 계정(관리자 dev@/yv@, 데모 끗짱 ai@, 데모 청소년 youthpd@)을 준비해서 실제로 써보다가 발견한 문제들.

- **posts DELETE 정책 누락**: `pg_policies`로 확인해보니 `posts` 테이블에 insert/update/select 정책만 있고 delete 정책이 DB에 실제로 만들어진 적이 없었음(스키마 파일엔 있었는데 실행이 안 됐던 것). RLS 테이블에 DELETE 정책이 없으면 관리자를 포함해 누구든 항상 0건 삭제로 막힘 — `posts_delete` 정책(글쓴이 본인 또는 관리자) 추가
- **공지 상단 고정 해제 기능**: 끗짱이 쓰는 글(`type='notice'`)은 항상 목록 맨 위 고정이었는데, 나중에 고정을 풀고 싶은 공지도 있을 수 있어 `posts.pinned` 컬럼(기본 true) 추가. 글쓰기/수정 시트에 "📌 목록 상단에 고정" 체크박스 추가, `_pinNotices()` 정렬 기준을 `type==='notice'`에서 `type==='notice' && pinned`로 변경(공지 타입/배지는 그대로 유지, 정렬만 영향)
- **게시글 수정 후 화면 반영 안 됨**: `kSubmitPost()`가 저장 후 항상 끗짱 화면(`kRenderReviewTab`)만 재렌더링하도록 하드코딩돼 있어서, 청소년 커뮤니티 화면(또는 관리자의 청소년 미리보기)에서 글을 수정하면 새로고침 전까진 반영이 안 보였음 — `deletePost()`처럼 `state.currentRole`에 따라 `renderComm()`/`kRenderReviewTab()`을 분기하도록 수정
- **모집중 상태에서 승인된 참여자에게 "신청하기"가 다시 뜨는 버그**: `openRoutineDetail()`이 승인 대기/거절 상태만 체크하고 "이미 승인된 참여자"인 경우를 전혀 체크하지 않고 있었음. 시작일 전이라 루틴 상태가 아직 `recruit`인 채로 있으면(승인돼도 `active` 전환은 시작일 기준), 이미 승인된 청소년에게도 무조건 "참여 신청하기" 버튼이 다시 보였음 — `isApprovedMine` 체크 추가해서 이 경우 "승인됐어요! 시작일부터 인증할 수 있어요" 안내로 대체
- DB 변경: `posts_delete` 정책 추가, `posts.pinned` 컬럼 추가 (둘 다 사용자가 SQL Editor에서 직접 실행, 완료)
- 검증: 브라우저에서 각 케이스(정상 삭제/재렌더링/승인된 참여자 화면)를 mock 데이터로 재현해 확인, 콘솔 에러 없음

## 최근 완료 (2026-07-29, 인증탭에 신청 시점부터 "곧 시작해요" 표시)

신청/승인은 됐는데 시작일이 아직 안 돼서 루틴 status가 `recruit`인 채로 남아있으면, 인증탭엔 아예 안 보여서 "신청이 잘 된 게 맞나" 헷갈리는 문제.

- `_myUpcomingRoutines()` 추가 — `recruit` 상태(자유루틴 제외)인 루틴 중 내 신청 상태가 대기중 또는 승인인 것만 필터 (거절된 건 제외)
- 인증탭에 "곧 시작해요" 섹션을 "오늘의 인증" 위에 추가. 승인 대기 중이면 "⏳ 승인 대기", 승인됐으면 `D-N`(또는 "오늘 시작") 뱃지로 구분 표시. 클릭하면 루틴 상세로 이동(인증 폼은 아직 안 열림)
- 처음엔 "승인된 것만" 보이게 했다가, 사용자 피드백으로 "신청만 해도(승인 대기 상태부터) 바로 보이면 좋겠다"고 해서 대기 상태까지 확장
- DB 변경 없음
- 검증: 브라우저에 대기중/승인/거절 3가지 상태의 루틴을 mock 주입 — 대기중은 "⏳ 승인 대기", 승인은 "D-3", 거절은 목록에서 제외되는 것 확인

## 최근 완료 (2026-07-29, 청소년 루틴탭에 "진행 중" 필터 추가)

청소년 루틴탭엔 전체/모집중/완료 칩만 있고 "진행 중"이 없어서, 지금 참여 중인 루틴만 따로 걸러볼 방법이 없었음.

- "진행 중" 칩 추가 — `renderRoutineGrid()`에 이미 구현돼 있던 `type==='active'` 필터(내가 참여 중인 active 루틴만, 끗짱의 조직 전체 스코핑과 달리 청소년은 원래 "내 참여" 기준) 로직을 그대로 연결만 하면 됐음
- DB 변경 없음
- 검증: 브라우저에 내가 참여 중인 루틴 + 남이 참여 중인 루틴을 mock 주입 — "진행 중" 필터에서 내 것만 보이는 것 확인, 칩 순서(전체/모집중/진행중/완료)도 확인

## 최근 완료 (2026-07-29, 루틴에 담당 끗짱 이름 표시 + 끗짱 홈·인증탭에 시작 전 루틴 미리보기)

- **담당 끗짱 이름 노출**: 청소년이 루틴 카드/상세 화면에서 어떤 끗짱이 담당인지 알 수 있도록, `loadRoutines()`에서 `led_by`를 프로필 이름으로 매핑한 `leaderName` 필드 추가. 루틴 카드(🙋 이름 끗짱)와 루틴 상세 화면 정보 칩에 표시
- **끗짱 홈/인증탭에 시작 전 루틴 미리보기**: 시작일 전이라 status가 아직 `recruit`인 담당 루틴은 홈/인증탭에 전혀 안 보여서 신청 현황을 알기 어려웠음. `_kUpcomingRoutines()` 추가(담당 루틴 중 `recruit` 상태, 자유루틴 제외) — 홈 화면 "진행 중 루틴" 목록과 인증탭 "담당 루틴" 목록 위에 "🗓 곧 시작해요" 섹션으로 `D-N`(또는 "오늘 시작")과 승인/대기 인원 수를 함께 표시
- DB 변경 없음
- 검증: 브라우저에서 담당 끗짱 이름이 카드/상세에 뜨는 것, 시작 4일 전 루틴이 홈·인증탭에 "D-4·2명 승인·대기 1명"으로 뜨는 것, 기존 진행중/빈 상태 화면은 그대로인 것 확인

## 최근 완료 (2026-07-29, 화면 "←" 버튼 뒤로가기가 홈으로 건너뛰던 구조적 버그 수정)

특정 화면(예: 인증글 상세)에서 "←"를 누르면 그 전 화면이 아니라 홈으로 바로 가버리는 문제. 특정 화면만의 문제가 아니라 앱 전체 뒤로가기 구조의 결함이었음.

- **원인**: 오버레이/시트를 열 때(`openOverlay`/`openSheet`)마다 `history.pushState()`로 히스토리를 쌓아서 뒤로가기를 흉내내는데, 화면의 "←" 버튼은 `history.back()`이 아니라 `closeOverlay()`/`closeSheet()`를 직접 호출해서 화면만 닫았음. 그러면 방금 쌓인 히스토리 항목은 브라우저에 그대로 남고 앱 내부 상태(`_navStack`)만 먼저 줄어들어, 나중에 진짜 뒤로가기(브라우저/제스처)를 누르면 밀린 항목들 때문에 여러 단계를 건너뛰고 홈까지 가버림. 앱의 모든 "←" 버튼(13곳)이 다 이 방식이라 어느 화면에서든 나타날 수 있는 문제였음
- **수정**: 화면 하나하나(13곳)를 고치는 대신, `closeOverlay()`/`closeSheet()` 자체가 실제로 무언가를 닫았을 때(=`_navRemoveModal()`이 true를 반환할 때)는 그 자리에서 `history.back()`도 함께 호출하도록 통일 — 이렇게 하면 "←" 버튼이든 다른 어떤 코드 경로로 닫히든 브라우저 히스토리와 `_navStack`이 항상 같이 움직임
- 이렇게 하면 `closeOverlay()`가 부른 `history.back()`이 나중에 popstate를 한 번 더 발생시키는데, 그걸 popstate 핸들러가 "진짜 사용자의 뒤로가기"로 착각해 한 단계 더 닫아버리지 않도록 `_selfTriggeredBackCount` 카운터로 구분(직접 닫을 때 +1, 그로 인한 popstate에서 -1하고 무시)
- DB 변경 없음
- 검증: 브라우저에서 오버레이 2개를 중첩으로 연 뒤 "←" 두 번을 시뮬레이션 — 첫 번째는 안쪽 오버레이만 닫히고 바깥 오버레이가 남아있는 것, 두 번째에 바깥까지 정상적으로 닫히는 것 확인. 별도로 `history.back()`을 직접 호출하는 "진짜" 뒤로가기 제스처도 여전히 정상 동작하는 것 확인

## 최근 완료 (2026-07-29, 일반 끗짱 홈 화면 UX 개선 + 루틴탭 관리자 전용화)

끗짱 홈/인증탭 "시작 전 루틴" 기능 추가 직후, 대화하며 나온 후속 개선.

- 홈 화면 "진행 중인 루틴" → "참여 중인 루틴"으로 제목 변경 (시작 전 루틴도 같이 보이니까)
- "🗓 곧 시작해요" 구분 헤더 제거 — D-day/진행중 배지만으로 충분히 구분되고, 목록이 하나로 자연스럽게 이어짐
- 홈 화면의 시작 전 루틴 카드에도 참여자 아바타 표시 추가 (기존 진행중 루틴 카드와 톤 맞춤)
- **루틴탭을 관리자(미리보기 포함, `loginType==='both'`) 전용으로 축소** — 일반 끗짱은 루틴 수정/승인 권한이 없고 담당 루틴 정보는 이미 홈·인증탭에서 다 볼 수 있어서, 조직 전체를 훑어보는 루틴탭이 굳이 필요 없다고 판단. `applyRole()`에서 `knav-routine` 표시 여부를 관리자 여부로 토글 — 일반 끗짱은 청소년과 동일한 4탭(홈/인증/커뮤니티/MY) 구성이 됨
- DB 변경 없음
- 검증: 브라우저에서 `applyRole('kkutjjang')`을 일반 계정/관리자 계정으로 각각 호출 — 일반 계정은 루틴탭이 숨겨지고 관리자는 그대로 보이는 것 확인

## 최근 완료 (2026-07-29, 끗짱 루틴탭 재복원 + 인증탭을 홈탭에 통합)

앞선 "루틴탭 관리자 전용화" 결정을 대화 중 재검토한 결과.

- **루틴탭 재복원**: 일반 끗짱이 루틴탭을 숨기니 다른 끗짱이 담당한 루틴의 인증글을 둘러볼 방법이 없어지는 트레이드오프가 드러나서, 루틴탭 숨김을 되돌림(전체 끗짱에게 다시 노출, 수정/생성 버튼은 기존처럼 관리자만)
- **인증탭 → 홈탭 통합**: 홈의 "참여 중인 루틴"(아바타+진행률 카드)과 인증탭의 "내 루틴 인증하기"/"오늘 인증 현황"이 사실상 같은 내용을 다르게 보여주던 중복이라, 인증탭을 아예 없애고 그 내용을 홈탭에 그대로 옮김. 홈 상단(요약 3칸/모임 배너/승인 대기 알림)은 그대로 두고, 그 아래에 "내 루틴 인증하기"(인증 버튼 카드, 시작 전 루틴은 D-day 뱃지)와 "오늘 인증 현황"(참여자별 인증 여부 목록)을 배치
- `kSwitchTab()`에서 `'cert'`를 `'home'`으로 리다이렉트해서 옛 알림 딥링크가 깨지지 않게 처리. `kRenderCertTab()`은 함수로 남겨서 `kRenderHome()`이 호출하는 구조로 정리, 중복 렌더링 없앰(`closeCertSuccess()`도 `kRenderHome()` 한 번만 호출)
- 끗짱 하단탭: 홈/루틴/커뮤니티/MY 4개 (인증 탭 제거)
- DB 변경 없음
- 검증: 브라우저에서 루틴탭이 다시 보이는 것, 인증 탭 네비 아이템이 사라진 것, 홈 화면에 "내 루틴 인증하기"·"오늘 인증 현황" 목록이 함께 뜨는 것, 옛 `kSwitchTab('cert')` 호출이 홈으로 정상 리다이렉트되는 것 확인

## 최근 완료 (2026-07-29, 홈탭 루틴 카드 클릭 시 인증글 히스토리 접근 + 참여자 목록 아코디언화)

인증탭을 홈탭에 합친 직후 나온 후속 피드백 2건.

- **루틴 클릭 시 그동안의 인증글을 볼 수 없던 문제**: "내 루틴 인증하기" 카드 전체의 클릭이 `selectCertRoutine()`(인증 폼 바로 열기)으로 연결돼 있어서, 카드를 눌러 그 루틴의 지난 인증글 목록(피드)을 볼 방법이 없어졌음(인증탭 통합 과정에서 청소년 쪽엔 있던 "카드 클릭=피드, 배지만 클릭=인증하기" 패턴이 끗짱 쪽엔 빠져있었음). 카드 클릭은 `openKRoutineFeed()`로 열리게 하고, "인증하기" 배지에만 별도로 `selectCertRoutine()`을 연결(`stopPropagation`)해서 청소년 쪽과 동일한 패턴으로 통일
- **"오늘 인증 현황"을 루틴별 드롭다운으로**: 여러 루틴을 담당하면 참여자가 한 줄로 쭉 나열돼 어느 루틴 소속인지 구분하기 번거로웠음. 루틴별로 그룹핑해서 헤더(이모지+제목+완료 인원 수)를 클릭하면 그 루틴 참여자 목록이 펼쳐지는 아코디언 방식으로 변경 — 별도 상태 관리 없이 클릭한 그룹의 다음 요소 `display`만 토글
- DB 변경 없음
- 검증: 브라우저에서 `openKRoutineFeed` 스텁 주입 후 카드 클릭 시 호출되는 것 확인, 루틴 2개로 그룹 2개가 접힌 상태로 렌더링되고 헤더 클릭 시 펼쳐지며 참여자 이름이 보이는 것 확인

## 최근 완료 (2026-07-30, "오늘 인증 현황"을 루틴 카드 안으로 재통합 + 담당자 본인 인증 그리드 누락 수정)

전날 만든 "인증하기 카드"와 "오늘 인증 현황(루틴별 아코디언)"이 결국 같은 루틴 목록을 두 번 보여주는 셈이라는 대화 끝에, 상의 후 진행.

- **참여자 목록을 별도 섹션이 아니라 각 루틴 카드 안에 통합**: "오늘 인증 현황" 섹션을 없애고, "내 루틴 인증하기"의 각 루틴 카드에 화살표(chevron) 아이콘을 추가 — 화살표만 누르면 그 루틴 참여자 목록이 카드 바로 아래로 펼쳐짐. 카드의 나머지 영역(제목 등) 클릭은 여전히 인증글 피드로, "인증하기" 배지는 여전히 인증 폼으로 — 세 클릭 영역이 서로 겹치지 않게 `stopPropagation`으로 분리
- **루틴 상세(피드) 화면에서 담당 끗짱/관리자 본인의 오늘 인증이 "오늘 인증 현황" 그리드에서 빠지던 문제**: 담당 끗짱은 `routine_participants`에 정식 등록된 참여자가 아니어도 자기 루틴에 바로 인증할 수 있는데, 그리드가 참여자 목록 기준으로만 그려져서 본인 인증은 그 아래 "전체 인증 기록"에만 보이고 그리드엔 안 잡혔음(실사용 중 발견 — 관리자 계정으로 인증 후 확인). 참여자 목록에 없는 사람이 오늘 인증한 경우를 찾아서 "담당 끗짱" 라벨의 별도 타일로 그리드에 추가
- DB 변경 없음
- 검증: 브라우저에서 화살표/카드/배지 세 클릭 영역이 서로 간섭 없이 각각 동작하는 것 확인. 참여자 목록에 없는 사용자의 오늘 인증 mock 주입 후 그리드에 "담당 끗짱" 라벨로 추가 타일이 뜨는 것 확인

## 최근 완료 (2026-07-30, 루틴탭 "전체" 정렬 순서를 참여중/모집중/진행중으로 변경)

청소년·끗짱 공통 요청: 루틴탭 "전체"에서 참여 중인 루틴이 맨 위, 그다음 모집중, 그다음(남의) 진행중 순으로 보이면 좋겠다는 피드백.

- 청소년 `renderRoutineGrid()`: 기존엔 모집중이 참여중보다 먼저였음 — `_rank()`를 고정 → 내가 참여 중(`isMine`) → 모집중 → 나머지 진행중 순으로 변경
- 끗짱 `kFilterRoutine()`: 같은 3단계 정렬 로직 추가(`isMineK`는 `led_by`가 본인인지로 판단), "전체" 필터가 완료(`done`) 루틴을 제외하도록 변경(청소년 쪽은 이미 그렇게 돼 있었음)
- DB 변경 없음
- 검증: 브라우저에 참여중/모집중/남의진행중/완료 루틴을 섞어서 mock 주입 — 청소년·끗짱 양쪽 다 참여중 → 모집중 → 진행중 순서로 정렬되고 완료는 "전체"에서 빠지는 것 확인

## 최근 완료 (2026-07-30, 끗짱 인증 상세 뒤로가기가 홈으로 건너뛰던 문제 + 자정~오전9시 타임존 버그 + 인증글 이름 가독성)

- **끗짱 루틴 피드 안에서 인증 상세를 열었을 때 뒤로가기가 피드를 건너뛰고 홈으로 가버리던 문제**: `openKRoutineFeed()`(루틴 피드)와 `openKCertDetail()`/`openCertFeedDetail()`(인증 상세)가 전부 같은 오버레이(`routineDetailOverlay`) 하나를 재사용해서 내용만 바꿔치기하는 구조였음. 청소년 쪽은 피드(`routineFeedOverlay`)와 인증 상세(`routineDetailOverlay`)가 서로 다른 오버레이라 문제없었지만, 끗짱 쪽은 같은 오버레이를 중첩해서 열다 보니 뒤로가기 한 번에 상세만 닫히는 게 아니라 오버레이 전체가 닫혀버렸음(지난번에 고친 히스토리 동기화 문제와는 별개의, 오버레이 재사용에서 오는 새로운 문제). 오버레이를 새로 열지 않고 "뒤로가기 한 번에 이전 내용으로 되돌리는" `subview` 개념을 히스토리 스택에 추가(`_navPushSubview`/`_navBackOrClose`/`_openInRoutineDetail`)해서 해결 — `openKRoutineFeed`를 순수 렌더 함수(`_renderKRoutineFeed`)와 오버레이 오픈으로 분리하고, `openKCertDetail`/`openCertFeedDetail`이 이미 열려있는 오버레이 안에서 호출되면 subview로 등록되게 함
- **자정~오전 9시 사이 "오늘 날짜"가 하루 전으로 계산되던 타임존 버그**: `new Date().toISOString().slice(0,10)`이 UTC 기준이라, 한국시간 자정부터 오전 9시(UTC 기준 자정) 사이엔 실제로는 오늘인데 어제 날짜로 계산됐음. 그래서 시작일이 "오늘"인 루틴이 새벽엔 아직 모집중 상태에 머물러 인증하기가 안 뜨는 것처럼 보였음(실사용 중 발견 — 새벽에 "오늘 시작"인데 왜 인증하기가 아니냐는 질문에서 원인 추적). `index.html`/`admin.html`에 로컬 타임존 기준 `_todayStr()` 헬퍼를 추가하고, 루틴 상태 자동전환·완주 판정·스트릭·인증 캘린더 등 "오늘 날짜"를 쓰던 11곳을 전부 교체. 인증 완료 여부 판정(`doneToday`)도 DB의 UTC `created_at`을 로컬 날짜로 변환 후 비교하도록 같이 수정
- **인증글 이름이 밝은 사진 위에서 잘 안 보이던 문제**: 인증글 그리드의 이름 라벨이 흰 글�자 + 반투명 그라데이션이라, 사진이 밝은 색이면(흰 배경 제품 사진 등) 대비가 부족했음. 그라데이션을 더 진하게(0.5→0.75) 하고 이름/날짜 텍스트에 그림자(`text-shadow`)를 추가해 어떤 배경에서도 읽히게 함 — "전체 인증 기록"(`_certFeedGridHtml`)과 "오늘 인증 현황"(`.feed-overlay`) 양쪽 다 적용
- DB 변경 없음
- 검증: 브라우저에서 `_todayStr()`이 실제 로컬 날짜(2026-07-30)를 정확히 반환하고 기존 UTC 방식(2026-07-29, 하루 밀림)과 차이나는 것 확인, 시작일=오늘인 mock 루틴이 `active`로 정상 전환되는 것 확인, 끗짱 피드→인증상세→뒤로가기 시 피드로 복원되고(오버레이 유지) 한 번 더 뒤로가기해야 완전히 닫히는 것 확인(청소년 쪽 회귀 없음도 확인), 밝은 배경 이미지에서 이름 라벨 가독성 향상 스크린샷으로 확인

## 최근 완료 (2026-07-30, 끗짱 홈/인증 현황 UX 개선 4건)

- **"오늘 인증 현황" 그리드에 실제 인증 사진 표시**: 인증 완료한 참여자 타일이 색상 placeholder만 보이고 실제 사진은 안 보이던 문제 — 참여자/담당 끗짱 타일 둘 다 오늘 인증한 사진의 첫 장을 배경 이미지로 표시(사진 없으면 기존 색상 placeholder 유지)
- **홈 "내 루틴 인증하기" 카드의 참여자 드롭다운에 콕 찌르기 추가**: 미인증 참여자 행에 "👋 콕" 버튼 추가(루틴 피드의 콕 찌르기와 동일한 `sendPoke()` 재사용), 행 클릭(인증 상세 열기)과 안 겹치게 분리
- **"오늘 인증 현황"에서 미인증 타일 제거 + 아무도 인증 안 했으면 고양이 문구**: 미인증 참여자를 회색 placeholder 타일로 보여주던 걸 없애고, 인증한 사람 타일만 쌓이는 피드처럼 보이게 변경. 아무도 인증 안 한 날은 "🐈 지나가던 고양이가 오늘의 첫 인증글을 기다리는 중..." 문구로 대체(미인증 명단은 홈 카드의 드롭다운에서 계속 확인 가능)
- **홈 상단 통계 카드 개편**: "승인 대기"(이제 일반 끗짱은 승인 권한이 없어 액션 불가능한 숫자)와 "진행 중 루틴"(그냥 개수라 동기부여가 약함) 대신, 담당 루틴이 1개면 "N일차"(그 루틴 제목과 함께)를, 2개 이상이거나 자유루틴이면 기존처럼 "진행 중 루틴 개수"로 폴백. 세 번째 칸은 "누적 인증 수"(담당 루틴들의 총 인증 횟수 합)로 교체
- DB 변경 없음
- 검증: 브라우저에서 사진 있는/없는 인증 타일 렌더링, 미인증자 콕 버튼 클릭 시 `sendPoke` 정확한 인자 호출, 아무도 인증 안 한 상태의 고양이 문구, 담당 루틴 1개일 때 "N일차"(시작일 기준 정확한 일수) 및 2개일 때 "진행 중 루틴" 폴백 + 누적 인증 수 합산 확인

## 최근 완료 (2026-07-30, 청소년 홈 상단에 "지금까지 만든 한끗" 누적 통계 추가)

청소년 홈에 동기부여용 조직 전체 누적 수치를 보여주면 좋겠다는 요청 — 대화로 지표를 좁혀나감(매일 9시 배치 vs 실시간 → 실시간이 더 간단해서 채택 / "오늘 인증한 사람 수"는 아이들이 밤 11시쯤 몰아서 올려서 낮엔 썰렁해 보일 수 있어 제외 / "루틴 수"는 임팩트가 약해서 제외 / 대신 "완주자 수", "소통수"(댓글 — 또래+끗짱 댓글 모두 포함, 고립·은둔 청소년에겐 끗짱 댓글도 중요한 소통이라는 맥락) 로 확정).

- 청소년 루틴(홈) 탭 최상단에 "지금까지 만든 한끗" 배너 추가 — **인증수**(전체 인증 건수) / **완주자**(완주 기준을 충족한 고유 참여자 수) / **소통수**(게시글+인증글 댓글 총합, 끗짱 댓글 포함)
- `loadHomeStats()` 신규 — 인증수·소통수는 `state.certFeed`/`state.posts`가 최근 500개로 캡(limit)돼 있어 그걸로 세면 실제보다 적게 나올 수 있어서, `certifications`/`post_comments`/`cert_comments`에 대해 정확한 COUNT 쿼리(`count:'exact', head:true`)로 직접 가져옴. 완주자 수는 `routines`/`routine_participants`가 캡 없이 전체를 불러오므로 이미 로드된 `state.routines`로 계산해도 정확함(비자유루틴은 인증 횟수≥완주기준, 자유루틴은 개인 기간 종료 여부로 판단)
- `loadProfileAndEnter()`/`refetchOnResume()`에서 호출 — 실시간으로 갱신(별도 배치/스케줄 불필요)
- DB 변경 없음
- 검증: 브라우저에서 COUNT 쿼리 응답과 완주 조건을 만족/불만족하는 참여자를 mock 주입 — 인증수·소통수가 COUNT 응답 그대로, 완주자 수가 조건 충족 인원만 정확히 집계되는 것 확인

## 최근 완료 (2026-07-30, 홈 통계 배너 일러스트 추가)

디자인이 밋밋하다는 피드백으로, "지금까지 만든 한끗" 배너 우측 하단에 반투명 새싹(🌱) 일러스트를 추가해 그 아래 홍보 배너와 시각적으로 구분되게 함 (처음엔 opacity 0.18로 넣었다가 거의 안 보여서 0.35로 조정).

## 최근 완료 (2026-07-30, QA 배치 — 모바일 입력란 겹침/인증글 CRUD 등 7건)

- **[모바일] MY 프로필 편집 휴대폰번호·생년월일 입력란 겹침**: 두 `<input>`이 같은 `.date-row` 그리드에 있는데 타입별(date/tel) 브라우저 기본 렌더링 높이가 모바일에서 서로 달라서 발생 — 공용 input CSS 규칙에 `height:40px` 명시해서 모든 입력 타입이 동일한 높이를 갖도록 통일
- **청소년 홈: 진행 중(active) 루틴을 비참여자가 봤을 때 아무 안내도 없던 문제**: `openRoutineDetail()`이 모집중/폐강/완료 상태만 분기 처리하고 있고 `active` 상태에 대한 분기가 아예 없어서, 이미 시작된 루틴을 비참여자가 보면 설명+포인트 배너만 뜨고 "이미 마감됐다"는 안내가 없었음(QA에서 "참여한 사람만 볼 수 있어요 메시지 안 나옴"으로 보고됨) — `else if(r.status==='active')` 분기 추가해서 "🗓 이미 시작돼서 모집이 마감됐어요" 안내 표시
- **[모바일] 가로 회전 시 안내 문구가 안 뜨던 문제**: `.rotate-lock-overlay`가 `orientation:landscape` + `hover:none` + `pointer:coarse` 조합으로 감지했는데, 일부 모바일 브라우저에서 이 조합이 안정적으로 안 잡힘 — `orientation:landscape` + `max-height:500px` 조합으로 변경(데스크톱 창은 이만큼 낮게 안 쓰는 경우가 많아 더 안정적으로 구분됨)
- **인증글 수정·삭제 불가능**: `certifications` 테이블에 update/delete RLS 정책이 아예 없었음(select/insert만 존재) — 본인 또는 관리자만 수정/삭제 가능하도록 정책 추가, 인증 상세 화면에 연필/휴지통 아이콘 추가(`editCert`/`deleteCert`, 본인 글에만 노출). 삭제 시 인증 횟수·완주 판정이 바뀌므로 루틴/인증피드 재조회 후 화면 닫음. 이미 지급된 나다움 포인트는 회수하지 않음(기존 정책과 동일)
- **MY탭 "인증수"에 올린 글 클릭 시 상세보기 안 됨(청소년)**: 현재 코드로 재현 시도했으나 정상 동작 — 오늘 세션 초반에 고친 오버레이/뒤로가기 구조 문제의 부산물로 이미 해결된 것으로 보임
- **끗짱 MY탭 "총 인증 수" 버튼 비활성화**: 클릭 핸들러 자체가 없었음(운영한 루틴/함께한 청소년/평균 완주율도 마찬가지로 비클릭) — "총 인증 수"만 클릭 시 홈 탭(인증 현황이 자세히 보이는 곳)으로 이동하도록 연결
- **끗짱 루틴탭 전체+완료 칩 모두에서 완료 루틴 보임**: 오늘 앞서 커밋(정렬 순서 변경, bf7c2d8)에서 "전체"가 완료 루틴을 제외하도록 이미 고쳐져 있어서 재확인만 함 — "완료" 칩에서 완료 루틴이 보이는 건 의도된 정상 동작
- DB 변경: `cert_update`/`cert_delete` 정책 추가 (사용자가 SQL Editor에서 직접 실행, 완료)
- 검증: 브라우저에서 입력란 높이 통일, active 상태 안내 문구, 가로/세로 각 뷰포트에서 회전 안내 표시 여부, 인증글 수정 시 DB 반영·삭제 시 권한 체크·화면 갱신, 루틴탭 전체/완료 필터 각각의 표시 내용을 모두 확인

## 최근 완료 (2026-07-30, 청소년 홈/루틴 상세 UX 다듬기 3건)

- **홈의 "오늘 미인증 루틴 N개 남았어요" 배너 제거**: 인증탭 하단 네비 배지가 이미 같은 정보를 보여주고 있어서 중복이라는 피드백 — `todayMissionArea` 배너/컨테이너 제거, 네비 배지 갱신(`updateCertNavBadge`)은 그대로 유지
- **루틴 카드 썸네일과 상세 화면 이미지 비율 불일치**: 카드는 `height:132px`(화면폭에 따라 비율이 들쭉날쭉), 상세는 `height:150px`(전체 폭 기준이라 훨씬 더 와이드하게 크롭)로 서로 달라서 같은 사진이 두 화면에서 다르게 잘려 보였음 — 둘 다 `aspect-ratio:4/3`으로 통일해서 화면 크기와 무관하게 항상 같은 비율로 보이게 함
- **루틴 상세의 "인증 시 사진 첨부 필수예요" 안내 제거**: 사진 없이는 인증 폼 자체가 제출을 막아서 이미 강제되고 있는 규칙이라 중복 안내라는 피드백으로 배너 삭제
- DB 변경 없음
- 검증: 브라우저에서 배너 제거 후에도 네비 배지가 정상 갱신되는 것, 카드/상세 두 화면의 이미지가 동일한 4:3 비율로 렌더링되는 것 스크린샷으로 확인

## 최근 완료 (2026-07-30, 청소년 인증탭 하단에 "인증 팁" 고정 섹션 추가)

인증탭 하단에 빈 공간이 많다는 피드백 — 논의 끝에 실용적인 인증 팁/가이드를 고정으로 두기로 함(안내 방향 여러 개 중 "인증 팁/가이드"로 확정, 배치는 실행 콘텐츠 아래).

- `renderCertTab()` 마지막에 "💡 인증 팁" 섹션 고정 추가 — 사진/시간대/소감/댓글/연속기록 5개 팁
- 처음엔 "짧게라도 소감을 남기면..."이라고 썼다가, 소감이 실제로는 필수 입력값이라 "짧게"를 강조하면 성의 없이 쓰는 걸 유도할 수 있다는 지적으로 "그날의 기분이나 생각을 솔직하게 담아보세요"로 수정
- 댓글/소통 관련 팁("다른 친구의 인증글에 댓글로 응원을 남겨보세요")도 추가로 반영
- DB 변경 없음
- 검증: 브라우저에서 팁 5개가 순서대로 렌더링되고 마지막 항목만 구분선이 없는 것, 문구 수정 반영 스크린샷으로 확인

## 최근 완료 (2026-07-30, 홈 통계 "인증수/소통수"가 계정마다 다르게 보이던 문제 수정)

모바일(일반 계정)과 PC(관리자 계정)에서 "지금까지 만든 한끗" 숫자가 다르게 보인다는 제보로 원인 추적.

- **원인**: `certifications`/`cert_comments`의 SELECT RLS 정책이 "그 루틴 참여자만 볼 수 있게" 제한돼 있어서(2026-07-25 보안 수정), 클라이언트에서 `count(*)` 쿼리를 하면 RLS 때문에 로그인 계정이 실제로 "볼 수 있는" 행만 세어짐 — 일반 계정은 자기 루틴 것만, 관리자는 `is_admin()`으로 전체가 다 세어져서 같은 통계가 계정 권한에 따라 다르게 나왔음
- RLS를 우회해 항상 같은 진짜 전체 개수만 돌려주는 `get_home_stats()` SECURITY DEFINER 함수 추가(개별 인증글/댓글 내용은 노출하지 않고 집계 숫자만 반환하므로 안전) — `loadHomeStats()`가 직접 COUNT 쿼리 대신 이 RPC를 호출하도록 변경. 완주자 수는 원래부터 `routines`/`routine_participants`가 RLS 제한이 없어서 그대로 둠(문제 없었음)
- DB 변경: `get_home_stats()` 함수 추가 (사용자가 SQL Editor에서 직접 실행, 완료)
- 검증: 브라우저에서 `sb.rpc` 응답을 mock 주입 — 반환된 집계값이 그대로 화면에 반영되는 것 확인

## ⚠️ 임시 조치 — 심사 끝나면 반드시 되돌릴 것 (2026-07-30)

지원사업 심사위원 체험용 계정의 실제 데이터가 적어 홈 상단 통계가 휑해 보이는 문제 — **실제 값이 아니라 화면 표시용 고정값**으로 임시 대체함(사용자 명시적 요청, 데모 목적 한정 — 보고서·신청서 등 실제 성과 지표로는 절대 이 숫자를 쓰지 말 것).

- `index.html`의 `DEMO_STATS_OVERRIDE = { certCount: 132, completers: 55, commCount: 350 }` — `loadHomeStats()`가 실제 계산값(`certCount`/`commCount`/`completers.size`) 대신 이 값을 화면에 표시하도록 덮어씀
- 실제 계산 로직은 그대로 남아있어서, 심사 끝나면 `DEMO_STATS_OVERRIDE.xxx` 참조 3곳을 원래 변수(`certCount`/`completers.size`/`commCount`)로 되돌리기만 하면 됨(코드에 큰 경고 주석으로 표시해둠)
- 검증: mock RPC 응답/실제 데이터와 무관하게 화면엔 132/55/350이 고정으로 뜨는 것 확인

## 최근 완료 (2026-07-30, 끗짱 루틴 상세/카드 UX 개선 2건)

- **일반 끗짱 루틴 상세에 대기중 신청자가 안 보이던 문제**: 승인/거절 권한이 없는 일반 끗짱은 "참여자 목록"에 승인된 사람만 보여서, 신청이 들어왔는지 자체를 몰랐음 — 승인 대기중 신청자도 읽기 전용으로 같이 보이게 하고(⏳ 대기중 뱃지), 승인 대기 인원 수 배지도 관리자 화면과 동일하게 표시. 거절된 신청자는 계속 제외
- **루틴 카드 "내 담당" 배지가 사진과 겹치고 다른 배지랑 자리가 겹치는 문제**: 카드 이미지 위에 진한 파란색으로 겹쳐서 표시되던 걸 없애고, 카드 본문 하단(신청 인원 배지와 같은 줄, 오른쪽 정렬)에 노란색 "MY" 배지로 이동 — 신청자가 없어도 내 담당 루틴이면 항상 표시됨
- DB 변경 없음
- 검증: 브라우저에서 승인/대기/거절 신청자를 섞어 mock 주입 — 일반 끗짱 화면에 승인+대기만 표시되고 거절은 제외되는 것, MY 배지가 신청 배지와 같이/단독으로 정상 표시되고 남의 루틴엔 안 뜨는 것 확인

## 최근 완료 (2026-07-30, 커뮤니티 "공지"는 관리자만 등록 가능하도록 분리)

- **버그**: 일반 끗짱이 커뮤니티에 글을 쓰면 전부 `type='notice'`(공지)로 등록돼서 목록 상단 고정/공지 배지가 무조건 붙던 문제(QA 발견)
- **수정**: 공지는 관리자(유스보이스)만 등록 가능하도록 하고, 일반 끗짱 글은 새 타입 `kpost`("소식")로 분리
  - `kSubmitPost()`: 관리자 여부(`state.loginType==='both'`)에 따라 insert 시 `type`을 `notice`/`kpost`로 분기, 고정(`pinned`)도 관리자만 실제 값 전송(비관리자는 항상 false)
  - 글쓰기 바텀시트의 "📌 목록 상단에 고정" 체크박스 행(`kPostPinnedRow`)을 비관리자에게는 숨김 처리(`openKWriteSheet`/`editPost` 양쪽)
  - `kRenderReviewTab()`의 공지/후기 2종 하드코딩 필터를 `type` 일치 여부로 일반화해서 새 타입도 자동 대응
  - 청소년/끗짱 커뮤니티 필터 칩에 "소식" 추가, 글 상세 제목(`rdTitle`)과 "내가 쓴 글" 배지를 3종(공지/소식/후기) 분기로 확장
- DB 변경: `posts.type` CHECK 제약에 `'kpost'` 추가 — `supabase-schema.sql` [마이그레이션 2026-07-30c]
- 검증: 브라우저에서 관리자/일반 끗짱 각각으로 `kSubmitPost()` 호출 — 관리자는 `type:'notice'`+체크된 `pinned`로 전송, 일반 끗짱은 `type:'kpost'`+`pinned:false`로 전송되는 것과, 고정 체크박스 행이 관리자에게만 보이는 것을 fetch 가로채기로 확인

## 최근 완료 (2026-07-30, 관리자 루틴 목록 기본 정렬 + 시작일 미룬 진행중 루틴이 계속 "진행 중"으로 남던 문제 수정)

- **관리자 루틴 목록 기본 정렬**: 정렬 헤더를 클릭하지 않은 기본 상태에서 등록 순서 그대로 보이던 걸, 모집중 → 진행중 → 완료 → 폐강 순으로 묶고 같은 상태 안에서는 시작일 최신순으로 보이도록 `_defaultSortRoutines()` 추가(`admin.html`). 열 헤더 클릭 정렬은 그대로 유지
- **버그**: 진행중(active)으로 넘어간 루틴의 시작일을 관리자가 미래 날짜로 수정해도 계속 "진행 중"으로 표시되던 문제 — 날짜 기반 상태 자동 보정 로직이 `recruit→active`/`active→done` 전환만 처리하고 `active→recruit`(시작일이 미래로 바뀐 경우) 역방향 전환은 빠져있었음
- **수정**: `admin.html`/`index.html` 양쪽의 자동 보정 로직에 `status==='active' && start_date > today → 'recruit'` 케이스 추가 — 페이지 로드 시 자동으로 DB에도 반영됨(별도 마이그레이션 불필요)
- DB 변경 없음
- 검증: node로 상태 전환 로직 단위 테스트 — 시작일을 내일로 수정한 진행중 루틴이 모집중으로, 아직 진행 기간 안인 루틴은 그대로 진행중, 이미 종료일 지난 루틴은 완료로 정확히 계산되는 것 확인

## 기술 부채 (급하지 않음)

- `index.html` 단일 파일 240KB+ — 필요시 CSS/JS 분리 검토
- admin.html 데이터 로딩이 전체 테이블 select 방식 — 참가자 수천 명 규모 되면 페이지네이션 필요
- `supabase-schema.sql`의 마이그레이션이 실제 DB에 다 적용됐는지 가끔 의심해볼 것 (routine-covers 버킷 누락 사고 전례, posts_delete 누락 사고도 이번에 추가로 확인됨)
